Charcoal drawing on plain white paper, landscape orientation, aspect ratio 1024:572 (wide, roughly 16:9). Hand-drawn, sketchy, loose charcoal strokes, no color, no shading beyond charcoal grey. Pure white background, no border. This illustration DOES use labels and arrows, hand-lettered in rough charcoal capitals, sparse and clear. Each service is a hand-sketched crate / box with its name and port hand-lettered on it; related crates sit inside a hand-drawn dashed enclosure with a hand-lettered group name; connections are charcoal arrows with short hand-lettered labels. Title hand-lettered across the top: "OFF GRID CONSOLE — BUNDLED STACK (COMPOSE PROFILES = CAPABILITIES)".

CENTRE: three crates drawn heavier than the rest, inside a solid hand-drawn box labelled "REQUIRED CORE" — "OFF GRID CONSOLE :3000", "AI GATEWAY :7878 (SEPARATE PROCESS)", "POSTGRES — STATE + AUDIT". Arrows from the console to the gateway "INFERENCE" and to Postgres "STATE + AUDIT".

AROUND THE CORE, dashed enclosures orbiting it, each a compose profile, each joined to the console by one labelled arrow:
- "DATA": "SEAWEEDFS :8333", "QDRANT :6333 (OPT)" — arrow "DATASETS"
- "SECRETS": "OPENBAO :8200" — arrow "GET SECRETS"
- "GUARDRAILS": "PRESIDIO-ANALYZER :5002", "PRESIDIO-ANONYMIZER :5001" — arrow "PII SCAN"
- "POLICY": "OPA :8181" — arrow "ALLOW / DENY"
- "IDENTITY": "KEYCLOAK :8080" — arrow "OIDC LOGIN"
- "OBSERVABILITY": "OTEL :4318", "VICTORIAMETRICS :8428", "VICTORIALOGS", "JAEGER :16686" — arrow "OTLP SPANS"
- "LINEAGE": "MARQUEZ :9000", "MARQUEZ-WEB :3001" — arrow "OPENLINEAGE"
- "LLMOPS": "LANGFUSE :3030", "WORKER", "CLICKHOUSE", "MINIO", "REDIS" (small note "5 CONTAINERS") — arrow "LLM TRACES"
- "AGENTS": "TEMPORAL :7233", "TEMPORAL-UI :8081" — arrow "DURABLE RUNS"
- "CACHING": "REDIS :6379" — arrow "CACHE"
- "SIEM": "OPENSEARCH :9200", "DASHBOARDS :5601" — arrow "SHIP LOGS"
- "FLAGS": "UNLEASH :4242" — arrow "FLAGS"
- "BI": "SUPERSET :8088" — arrow "EXPLORE DATA"
- "AGENT QA": "EVIDENTLY :8001", "RAGAS :8002" — arrow "EVALS / DRIFT"
Inside the CONSOLE crate, two small hand-lettered tags: "LANCEDB — ON-DISK VECTORS (NO CONTAINER)" and "PROVENANCE + SANDBOX — IN-PROCESS / DOCKER (NO CONTAINER)".

A hand-lettered caption bar across the bottom: "EACH ENCLOSURE IS ONE COMPOSE PROFILE. RUN ONLY WHAT YOU LICENSE — make up · make policy · make guardrails · make llmops. THE THREE CORE CRATES ARE THE ONLY HARD REQUIREMENTS."

Style: rough charcoal throughout, expressive but readable; labels hand-lettered in loose charcoal capitals with the real port numbers, kept sparse. The three core crates are drawn heaviest so the reader sees what is mandatory vs optional. Every crate is a real container with its real port.

The meaning the viewer should sense: this is exactly what is bundled, grouped by compose profile that maps one-to-one to a capability; only the console, the gateway and Postgres are required; everything else is an optional profile you bring up only if you use it.
