Charcoal drawing on plain white paper, landscape orientation, aspect ratio 1024:572 (wide, roughly 16:9). Hand-drawn, sketchy, loose charcoal strokes, no color, no shading beyond charcoal grey. Pure white background, no border. This illustration DOES use labels and arrows, hand-lettered in rough charcoal capitals, sparse and clear. Components are hand-sketched boxes with a hand-lettered name; connections are charcoal arrows with short hand-lettered labels. Title hand-lettered across the top: "OFF GRID CONSOLE — CAPABILITY PORTS, SWAPPABLE ADAPTERS".

LEFT SIDE: a tall sketched cabinet drawn in charcoal labelled "OFF GRID CONSOLE CORE — CALLS getX() PORTS, NEVER A TOOL DIRECTLY". Down its right edge, a column of small hand-drawn sockets / notches, each labelled with a capability name. A hand-lettered caption beneath the cabinet: "SWAP = ONE ENV VAR  OFFGRID_ADAPTER_<CAP>".

For each socket, a horizontal row to the right: a charcoal arrow labelled "ACTIVE" to the DEFAULT adapter (drawn as a box with a small plug seated in the socket), then a lighter dashed arrow labelled "OR SWAP" to one or two alternative adapter boxes (plugs drawn pulled out, waiting). Rows top to bottom:
- "INFERENCE"     → "OFF GRID GATEWAY (ALWAYS)"      (tiny note "SINGLE EGRESS, NO SWAP")
- "RETRIEVAL"     → "LANCEDB"        or "PGVECTOR · QDRANT"
- "GUARDRAILS"    → "REGEX CHECKS"   or "MICROSOFT PRESIDIO"
- "POLICY"        → "RBAC + ABAC"    or "OPEN POLICY AGENT"
- "IDENTITY"      → "AUTH.JS"        or "KEYCLOAK"
- "SECRETS"       → "PROCESS ENV"    or "OPENBAO"
- "OBSERVABILITY" → "OTEL"           or "LANGFUSE · SIGNOZ"
- "LINEAGE"       → "NONE (IMPLICIT)" or "MARQUEZ"
- "AGENT-RUNTIME" → "IN-PROCESS"     or "TEMPORAL (DURABLE)"
- "CACHING"       → "IN-PROCESS"     or "REDIS"
- "SIEM"          → "AUDIT STORE"    or "OPENSEARCH"
- "FLAGS"         → "ENV FLAGS"      or "UNLEASH"
- "EVALS"         → "GOLDEN SET"     or "PROMPTFOO · RAGAS"
- "DRIFT"         → "PSI (NATIVE)"   or "EVIDENTLY"
- "PROVENANCE"    → "ed25519 / HMAC" or "C2PA · SIGSTORE"
- "SANDBOX"       → "NONE (NO-EXEC)" or "DOCKER · E2B"

A hand-lettered caption bar across the bottom: "FALLBACK — IF AN OSS ADAPTER IS UNREACHABLE, THE PORT REVERTS TO THE FIRST-PARTY DEFAULT. A SWAP IS NEVER A HARD DEPENDENCY."

Style: rough charcoal throughout, expressive but readable; labels hand-lettered in loose charcoal capitals, kept sparse. The plug-and-socket metaphor (default plug seated, OSS plug waiting) should make the eye read 'default is first-party; OSS is the swap-in'. Every adapter is a real, named tool.

The meaning the viewer should sense: the console depends on capability ports, not on any single tool; each capability ships a first-party default and one or two real open-source swap-ins, selectable by a single environment variable; the console always falls back to its own default if the OSS is down.
