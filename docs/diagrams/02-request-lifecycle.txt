Charcoal drawing on plain white paper, landscape orientation, aspect ratio 1024:572 (wide, roughly 16:9). Hand-drawn, sketchy, loose charcoal strokes, no color, no shading beyond charcoal grey. Pure white background, no border. This illustration DOES use labels and arrows, hand-lettered in rough charcoal capitals, sparse and clear. Components are hand-sketched boxes with a hand-lettered name; connections are charcoal arrows with a short hand-lettered label. Title hand-lettered across the top: "OFF GRID CONSOLE — ONE GATEWAY, EVERY REQUEST GOVERNED".

The scene reads left to right along a single bold charcoal arrow labelled "EVERY PROMPT · EVERY RESPONSE".

FAR LEFT: a small figure at a desk labelled "CLIENT / AGENT — desktop node or use case", an arrow right labelled "PROMPT (TEXT / IMAGE)".

CENTRE: a large fortified clearing-house drawn with thick charcoal walls, one inbound door on the left and one outbound door on the right, labelled "GATEWAY :7878 — MULTIMODAL · MCP · THE SINGLE EGRESS". Inside, a vertical stack of inline inspection stations, each a small sketched shed with a hand-lettered label, connected top-to-bottom by short arrows:
- "INPUT GUARDRAIL → PRESIDIO — DETECT / REDACT PII" (a figure holding a sieve)
- "POLICY → OPA — ALLOW / DENY BY USER + PURPOSE" (a figure with a rulebook)
- "ROUTE → ROUTER — INTENT: KB / DB / TOOL" (a signpost with three forks)
- "RETRIEVE → BRAIN / LANCEDB — VERSIONED KNOWLEDGE" (a vault of numbered boxes)
- "GENERATE → LOCAL MODEL (GEMMA) — ON-DEVICE"
- "OUTPUT CHECK → GROUNDING — VERIFY EVERY CITATION" (a stamp coming down)

RIGHT of the clearing-house: a box "RESPONSE — ANSWER + CITATIONS", arrow out labelled "GROUNDED ANSWER" returning to a small "CLIENT / AGENT" figure receiving a sealed envelope.

A branch off ROUTE labelled "TOOL CALL" to a box "MODEL ROUTING — LOCAL DEFAULT; CLOUD ONLY IF POLICY ALLOWS", with a small lever tag "EGRESS LEASH".

ABOVE the clearing-house: a heavy lever on a high mount, cord running into the roof, labelled "KILL SWITCH — ONE PULL HALTS THE NODE".

ACROSS THE BOTTOM: a long unrolled charcoal scroll spanning the whole width labelled "AUDIT LOG (POSTGRES) — USER · PURPOSE · MODEL · TOKENS · VERDICT · CHECKS — APPEND-ONLY, SOURCE OF TRUTH". Thin arrows drop from each inspection station into the scroll labelled "LOG".

ALONG THE RIGHT EDGE: a vertical rail with tap arrows from the gateway — "OTEL → LANGFUSE / JAEGER — TRACES, COST", "MARQUEZ — LINEAGE: SOURCE → ANSWER", "ed25519 SIGN — TAMPER-EVIDENT EXPORT".

Style: rough charcoal throughout, expressive but readable; labels hand-lettered in loose charcoal capitals, kept sparse. The bold left-to-right arrow through the clearing-house and the long bottom scroll frame the whole scene. Every box is a real, named piece of software (Presidio, OPA, Router, LanceDB, gemma, Grounding, Postgres, Langfuse, Marquez).

The meaning the viewer should sense: one gateway sits between every client and every model; each request is scanned for PII, checked against policy, routed, answered from versioned knowledge, citation-verified, and written to one append-only audit log — traces, lineage and signatures captured on the side, all on-device and all stoppable with one switch.
