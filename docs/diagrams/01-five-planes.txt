Charcoal drawing on plain white paper, landscape orientation, aspect ratio 1024:572 (wide, roughly 16:9). Hand-drawn, sketchy, loose charcoal strokes, no color, no shading beyond charcoal grey. Pure white background, no border. This illustration DOES use labels and arrows, hand-lettered in rough charcoal capitals, sparse and clear. Every component is a hand-sketched rectangular box with its name hand-lettered inside and one tiny line under it; connections are charcoal arrows with a short hand-lettered label. Title hand-lettered across the top in bold charcoal capitals: "OFF GRID CONSOLE — THE FIVE-PLANE AGENTIC STACK".

The scene is a cross-section of a single tall building drawn in charcoal, with five stacked floors. A thick charcoal vertical spine runs down the left edge through all five floors, hand-lettered up its length "OFF GRID CONSOLE — ONE CONTROL PLANE", with short charcoal arrows branching right into each floor labelled "GOVERN / AUDIT". The floors, BOTTOM to TOP, each with a hand-lettered floor label on the left margin:

FLOOR 1 (bottom) "DATA PLANE": sketched boxes — "POSTGRES — state + audit", "CONNECTORS — pull source data", "PRESIDIO MASKING — redact PII", "DATASETS — classified, erasable", "SEAWEEDFS — object store". Charcoal arrow CONNECTORS→MASKING "RAW ROWS", MASKING→DATASETS "MASKED".

FLOOR 2 "CONTROL PLANE": boxes — "AUDIT LOG — source of truth" (drawn as an unrolled scroll), "OPA — allow / deny", "PRESIDIO — pii scan", "OPENBAO — secrets", "KEYCLOAK — SSO", and a heavy lever labelled "KILL SWITCH". Arrows from OPA and PRESIDIO down into the AUDIT scroll labelled "FINDINGS".

FLOOR 3 (centre, drawn heaviest) "AI PLANE": a large fortified box in the middle "THE ONE GATEWAY — multimodal · MCP · all inference". Around it: "BRAIN / LANCEDB — retrieval", "ROUTER — KB / DB / TOOL", "GROUNDING — verify citations", "TEMPORAL — durable agent runs". Arrows ROUTER→BRAIN "QUERY", BRAIN→GROUNDING "SOURCES", and a bold arrow through the gateway "EVERY PROMPT + RESPONSE".

FLOOR 4 "REGULATORY PLANE": boxes — "REPORT PACKS — IRDAI / RBI / SEBI / DPDP" (drawn as sealed folders), "ed25519 SIGNING — tamper-evident", "FRAMEWORK COVERAGE", "DPIA / EVIDENCE". Arrow from the AUDIT scroll up to REPORT PACKS labelled "EVIDENCE".

FLOOR 5 (top) "CONSUMPTION": boxes — "FLEET CONTROL — enroll · command · analytics", "AGENTS — claims / KYC / SOP", "DESKTOP NODES — on-device". Arrow AGENTS down into the gateway "ALL CALLS", arrow FLEET→NODES "POLICY PUSH".

Along the right edge, a thin hand-drawn rail spanning floors 2-5 labelled "OTEL → LANGFUSE / JAEGER (TRACES) · MARQUEZ (LINEAGE)" with small tap arrows into the gateway and the audit scroll.

Style: rough charcoal throughout, expressive but readable; labels hand-lettered in loose charcoal capitals, kept sparse — only the ones named. The fortified gateway on the AI floor is the heaviest structure; the single spine clearly threads all five floors. Every box is a real, named piece of software.

The meaning the viewer should sense: Off Grid Console is one control plane holding all five layers of the agentic stack, data at the base, consumption on top, a single gateway at the heart — everything governed and audited in one place, on the user's own machines.
