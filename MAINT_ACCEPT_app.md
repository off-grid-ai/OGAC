# Maintainability accept-list — src/app/ slice

SonarCloud MAINTAINABILITY findings under `src/app/` deliberately NOT fixed, because a
behavior-preserving mechanical fix isn't possible/safe. Format: `rule | file:line | justification`.
Route handlers (`src/app/api/**`) are behavior-sensitive; anything non-trivial there is accept-listed
rather than risk a runtime change. Grouped by slice (A=admin API, B=other API, C=console pages
build/data/gateway, D=other console pages + landing/signin).

## Slice A — admin API routes
S6551 | src/app/api/v1/admin/access/idp/route.ts:42 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/access/idp/route.ts:43 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/access/idp/route.ts:44 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/access/idp/route.ts:45 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/access/idp/route.ts:46 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/access/idp/route.ts:47 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/access/realm/route.ts:51 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/copilot/suggest-expectations/route.ts:25 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/data-assets/[id]/classifications/route.ts:40 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/data-assets/[id]/classifications/route.ts:48 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/data-assets/[id]/retention/route.ts:34 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/data-assets/[id]/retention/route.ts:43 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/data-assets/[id]/route.ts:27 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/data-assets/[id]/route.ts:31 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/data-assets/[id]/route.ts:32 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/data-assets/[id]/route.ts:33 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/data-assets/[id]/route.ts:34 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/data-assets/[id]/route.ts:35 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/data-assets/[id]/route.ts:36 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/data-assets/[id]/route.ts:45 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/data-assets/[id]/route.ts:46 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/data-assets/[id]/route.ts:50 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/data-assets/route.ts:21 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/data-assets/route.ts:30 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/data-assets/route.ts:31 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/data-assets/route.ts:32 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/data-assets/route.ts:33 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/data-assets/route.ts:34 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/data-assets/route.ts:35 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/data-domains/[id]/route.ts:35 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/data-domains/[id]/route.ts:40 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/data-domains/[id]/route.ts:45 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/data-domains/[id]/route.ts:52 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/data-domains/[id]/route.ts:54 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/erasure-requests/route.ts:32 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/evals/run/route.ts:27 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/fleet/policies/route.ts:43 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/governance/[id]/route.ts:25 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/governance/[id]/route.ts:27 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/governance/[id]/route.ts:31 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/lineage/tags/route.ts:27 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/pipelines/[id]/route.ts:39 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/pipelines/[id]/route.ts:40 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/pipelines/[id]/route.ts:41 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/pipelines/[id]/route.ts:42 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/pipelines/[id]/route.ts:43 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/pipelines/[id]/route.ts:48 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/pipelines/route.ts:41 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/teams/[id]/route.ts:41 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/teams/[id]/route.ts:42 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S6551 | src/app/api/v1/admin/teams/route.ts:39 | Defensive String(unknown) coercion of a loosely-typed (Record<string,unknown>) request-body field / audit-resource id. Narrowing to typeof-string would DROP non-string values (behavior change) rather than preserve current stringification. Route-handler behavior-preserving accept per RoE.
S3776 | src/app/api/v1/admin/access/clients/route.ts:30 | Route-handler cognitive complexity (CC 19). Reduction requires structural control-flow extraction of an auth/validation/dispatch handler — not a mechanical, behavior-preserving change. Accept per RoE (API routes are behavior-sensitive).
S3776 | src/app/api/v1/admin/access/service-clients/provision/route.ts:31 | Route-handler cognitive complexity (CC 33). Reduction requires structural control-flow extraction of an auth/validation/dispatch handler — not a mechanical, behavior-preserving change. Accept per RoE (API routes are behavior-sensitive).
S3776 | src/app/api/v1/admin/apps/runs/[id]/review/route.ts:38 | Route-handler cognitive complexity (CC 19). Reduction requires structural control-flow extraction of an auth/validation/dispatch handler — not a mechanical, behavior-preserving change. Accept per RoE (API routes are behavior-sensitive).
S3776 | src/app/api/v1/admin/data-assets/[id]/route.ts:18 | Route-handler cognitive complexity (CC 33). Reduction requires structural control-flow extraction of an auth/validation/dispatch handler — not a mechanical, behavior-preserving change. Accept per RoE (API routes are behavior-sensitive).
S3776 | src/app/api/v1/admin/data-domains/[id]/route.ts:18 | Route-handler cognitive complexity (CC 20). Reduction requires structural control-flow extraction of an auth/validation/dispatch handler — not a mechanical, behavior-preserving change. Accept per RoE (API routes are behavior-sensitive).
S3776 | src/app/api/v1/admin/flags/[key]/variants/route.ts:9 | Route-handler cognitive complexity (CC 17). Reduction requires structural control-flow extraction of an auth/validation/dispatch handler — not a mechanical, behavior-preserving change. Accept per RoE (API routes are behavior-sensitive).
S3776 | src/app/api/v1/admin/pipelines/[id]/route.ts:23 | Route-handler cognitive complexity (CC 20). Reduction requires structural control-flow extraction of an auth/validation/dispatch handler — not a mechanical, behavior-preserving change. Accept per RoE (API routes are behavior-sensitive).
S3776 | src/app/api/v1/admin/tools/[id]/route.ts:7 | Route-handler cognitive complexity (CC 16). Reduction requires structural control-flow extraction of an auth/validation/dispatch handler — not a mechanical, behavior-preserving change. Accept per RoE (API routes are behavior-sensitive).

## Slice B — other (non-admin) API routes
S6551 | src/app/api/v1/chat/transcribe/route.ts:18 | `String(form?.get('filename') ?? 'audio.webm')` already wraps in String(). form.get() is string|File|null; narrowing to typeof==='string' would change the File-field edge behavior. Existing String() coercion is intentional; narrowing is not behavior-preserving.
S6551 | src/app/api/v1/chat/transcribe/route.ts:20 | `form.get('model') ? String(form.get('model')) : undefined` already String()-wrapped; same reasoning as :18 — behavior-preserving narrowing not available without altering the File-field edge case.
S6551 | src/app/api/v1/inbound/email/route.ts:122 | `String(input.subject ?? '')` — input is Record<string,unknown>, subject typed unknown. normalizeInboundEmail's str() guarantees a string at runtime, so String() is a no-op there; narrowing to typeof==='string'?...:'' changes the non-string fallback (String(x) vs '') and is not strictly behavior-preserving. Keep String().
S3776 | src/app/api/v1/chat/stream/route.ts:71 | Streaming handler (cognitive complexity 151). Per directive: do NOT refactor the streaming handler — risk of altering await/streaming/response behavior. Accept-listed.
S3776 | src/app/api/v1/chat/stream/route.ts:577 | Streaming handler (cognitive complexity 48). Same reasoning as :71 — accept-listed.
S3358 | src/app/api/v1/chat/stream/route.ts:113,219,441,567,568,742 | STALE scan: current file lines are single (non-nested) ternaries or not ternaries at all; file was refactored since the SonarCloud scan. No nested ternary present at reported locations. No edit.
S6582 | src/app/api/v1/chat/stream/route.ts:288,532,537 | STALE scan: current lines already use optional chaining (`skill?.`, `plan?.`) or are not `a && a.b` patterns; resolved by prior refactor. No edit.
S3358 | src/app/api/v1/chat/skills/route.ts:26 | STALE: inner ternary already extracted to `adminVisibility` const (line 26) and consumed at line 27. No nested ternary present.
S3358 | src/app/api/v1/gateway/providers/route.ts:49 | STALE: inner ternary already extracted to `configuredHealth` const (line 45), consumed at line 50. No nested ternary present.
S3358 | src/app/api/v1/studio/templates/route.ts:40 | STALE: inner ternary already extracted to `requestedVisibility` const (line 33), consumed at line 41. No nested ternary present.
S3776 | src/app/api/v1/studio/suggest/route.ts:16 | POST handler, complexity 16. Complexity is inherent (auth gate, body parse/validate, gateway fetch, JSON extraction, field-level guards). A clean reduction means extracting the parse/validate into a pure helper in src/lib (out of scope for this slice) — a local extraction is judgment-heavy and risks altering the gateway/response behavior. Accept-listed per route-handler policy.
S3776 | src/app/api/v1/gateway/logs/route.ts:24 | GET handler, complexity 18. Inherent request/filter/aggregation branching; behavior-preserving reduction would require a src/lib helper (out of scope). Accept-listed.
S3776 | src/app/api/v1/inbound/email/route.ts:33 | POST handler, complexity 16. Auth + normalize + app-vs-agent target branches + ABAC + audit. Behavior-preserving reduction needs src/lib helper extraction (out of scope). Accept-listed.
S3776 | src/app/api/v1/knowledge/collections/[id]/documents/route.ts:29 | POST handler, complexity 18. Multipart-vs-JSON input branches, file save, content resolution. Behavior-preserving reduction needs src/lib helper (out of scope). Accept-listed.
S3776 | src/app/api/v1/prompts/playground/route.ts:18 | POST handler, complexity 21. Inherent gateway-call + variant/parse branching. Behavior-preserving reduction needs src/lib helper (out of scope). Accept-listed.
S3776 | src/app/api/v1/triggers/[token]/route.ts:39 | POST handler, complexity 17. Token lookup + signature verify + app/agent dispatch + audit. Behavior-preserving reduction needs src/lib helper (out of scope). Accept-listed.

## Slice C — console pages (build / data / gateway / overview / provit)
S6479 | src/app/(console)/build/agents/[id]/runs/[runId]/page.tsx:35 | RunStep has no stable unique id (kind/label/detail/refs/ms); ordered/append-only "governed chain" — index IS the step identity. Fabricated key could change reconciliation.
S6479 | src/app/(console)/build/agents/[id]/runs/[runId]/page.tsx:62 | CheckResult has no stable unique id (name/verdict/detail/score) and names can repeat; keying on name could collide. Index retained per ROE.
S6479 | src/app/(console)/data/warehouse/[table]/page.tsx:151 | Raw warehouse query rows have no guaranteed id column; a rendered result set where row order is identity. Index retained per ROE.

## Slice D — console pages (insights / governance / operations) + landing / signin / features
S3358 | src/app/(console)/operations/runs/[id]/page.tsx:147 | Nested ternary is the JSX render branch (agent vs chat vs null); extracting to a const would lift a large JSX subtree — low value, readability-neutral. Behavior-preserving accept.
S6479 | src/app/(console)/operations/runs/[id]/page.tsx:82 | agent.steps have no stable unique id; index key is correct here (append-only ordered timeline). Fabricating a key risks changing reconciliation.
S6479 | src/app/(console)/operations/runs/[id]/page.tsx:105 | s.refs are bare strings with no stable id; index key appropriate.
S6479 | src/app/(console)/operations/runs/[id]/page.tsx:125 | agent.checks have no stable unique id; index key appropriate.
S3776 | src/app/(console)/operations/runs/[id]/page.tsx:19 | Page render CC driven by conditional JSX sections; splitting would fragment a cohesive server component with no behavior change and real regression risk in an async data page.
S6551 | src/app/signin/page.tsx:67 | formData.get('username') is FormDataEntryValue|null; already String()-wrapped (no-op for the text input). Narrowing to typeof==='string' would change the File-value edge, so it's not strictly behavior-preserving. Accept.
S6551 | src/app/signin/page.tsx:68 | Same as :67 for password — already String()-wrapped text input.
S6551 | src/app/signin/page.tsx:69 | Same as :67 for callbackUrl — already String()-wrapped text input.
S6479 | src/app/(console)/insights/evals/[id]/page.tsx:96 | Eval cases have no stable unique id; c.query can repeat across cases. Ordered result set — index is the row identity. Accept per RoE.
S6479 | src/app/(console)/insights/page.tsx:160 | Guardrail checks have no stable unique id (c.name can repeat); ordered badge list. Index appropriate. Accept per RoE.
S6479 | src/app/_landing/flow-diagram.tsx:100 | Beams map over a positional refs array (refs.slice(0,-1)); index IS the connector identity. No stable id exists. Accept per RoE.
S3776 | src/app/(console)/governance/guardrails/page.tsx:23 | Page-level CC 21 driven by conditional JSX sections + data fetching; a behavior-safe reduction needs component/logic extraction beyond a mechanical change. Accept per RoE (props marked readonly).
S3776 | src/app/(console)/governance/secrets/page.tsx:27 | CC reduced by extracting the status-banner chain + seal-status/tone/valueClass out of render; any residual CC over 15 is inherent page composition — no further behavior-safe mechanical reduction. Accept.
