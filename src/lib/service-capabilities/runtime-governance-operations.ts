import {
  defineCapability,
  type CapabilityGateInput,
  type ServiceCapabilityAudit,
} from '../service-capability-contract';

/**
 * Runtime/governance/operations lane of the canonical platform inventory.
 *
 * Only services with a bounded, versioned operator-outcome denominator appear in `AUDITS`.
 * The rest remain explicitly unaudited so the canonical projection renders “not audited”, never
 * an invented 0% or 100%. Existing LiteLLM and Presidio audits stay owned by the canonical legacy
 * registry here so each service has exactly one family owner.
 */

type CapabilitySpec = readonly [
  id: string,
  name: string,
  summary: string,
  uiHref: string,
  uiLabel: string,
  gap: string,
  gates: CapabilityGateInput,
];

interface AuditSpec {
  serviceId: string;
  serviceLabel: string;
  upstreamVersion: string;
  versionSource: string;
  denominatorSource: string;
  /** When THIS service was last audited. Per-service on purpose — see the default below. */
  auditedAt?: string;
  auditState?: ServiceCapabilityAudit['auditState'];
  auditStateEvidence?: string;
  summary: string;
  capabilities: readonly CapabilitySpec[];
}

function audit(spec: AuditSpec): ServiceCapabilityAudit {
  return {
    serviceId: spec.serviceId,
    serviceLabel: spec.serviceLabel,
    upstreamVersion: spec.upstreamVersion,
    versionSource: spec.versionSource,
    denominatorSource: spec.denominatorSource,
    // PER-SERVICE, not one date for the family. Re-auditing the Console and stamping today's date on
    // all twenty entries would claim twenty audits that never happened — the exact overstatement this
    // map exists to avoid. Services keep the date they were actually last looked at.
    auditedAt: spec.auditedAt ?? '2026-07-20',
    auditState: spec.auditState ?? 'stale',
    auditStateEvidence:
      spec.auditState === 'current'
        ? null
        : (spec.auditStateEvidence ??
          'The configured version is known, but the fleet ledger does not record the live image digest.'),
    summary: spec.summary,
    items: spec.capabilities.map((capability) => defineCapability(...capability)),
  };
}

const FLEET_EVIDENCE =
  '../onprem-fleet-orchestration/deploy/onprem/SERVICE_MAP.md and SERVER_STATE.md, verified 2026-07-20';
const PRESIDIO_ROUTE = '/governance/guardrails/overview';
const GUARDRAILS_ROUTE = '/governance/guardrails/overview';
const GUARDRAILS_PROTECTIONS_ROUTE = '/governance/guardrails/protections';
const GUARDRAILS_TEST_ROUTE = '/governance/guardrails/test';
const ROUTER_ROUTE = '/runtime/models/routing';

export const RUNTIME_GOVERNANCE_OPERATIONS_AUDITS = [
  audit({
    serviceId: 'console',
    serviceLabel: 'Console',
    auditedAt: '2026-08-03',
    upstreamVersion: 'Deployed release 10c00aa44e6d80843dd1f9b0fda1b312d9fbc8cd',
    versionSource: `live deploy/.deployed-sha on S1 verified 2026-08-03 → 10c00aa44e6d80843dd1f9b0fda1b312d9fbc8cd, signin 200; ${FLEET_EVIDENCE} release stamp`,
    denominatorSource:
      'src/app/(console) route tree; src/lib/tenancy-policy.ts; src/lib/adapters contracts',
    auditState: 'current',
    summary:
      'Bounded first-party denominator: authenticated operator access, management routes, tenant isolation, durable run dispatch, and the accountability records a DPO or CISO is asked for. Self-restart remains host-owned.',
    capabilities: [
      [
        'authenticated-control-plane',
        'Authenticated tenant control plane',
        'Open the governed management Console with tenant-scoped data and actions.',
        '/operations',
        'Open operations',
        '',
        [
          'yes',
          'The immutable Console release provides session and service-account authorization.',
          'yes',
          'NextAuth, tenancy, authorization, and repository seams are wired.',
          'yes',
          'All canonical domains are reachable through URL-driven Console routes.',
          'yes',
          'The authenticated integration harness exercised bank and insurance tenant routes.',
        ],
      ],
      [
        'tenant-management',
        'Tenant-isolated management state',
        'Create and manage product entities without crossing organization boundaries.',
        '/governance/access',
        'Manage access',
        '',
        [
          'yes',
          'The first-party release includes tenant-owned repositories and authorization rules.',
          'yes',
          'Database, claims, and policy adapters enforce organization scope.',
          'yes',
          'Domain management routes expose tenant-scoped entities and actions.',
          'yes',
          'Bank and insurance seed checks proved isolation of solution records.',
        ],
      ],
      [
        'policy-history',
        'Policy history and per-run policy version',
        'See what the rules said when a decision was made, not only what they say now.',
        '/governance/policies/history',
        'Open policy history',
        '',
        [
          'yes',
          'The release records an immutable version of the ruleset on every change.',
          'yes',
          'Snapshots are written inside the three rule mutators, so no route can leave a gap in the history.',
          'yes',
          'History is a Console route, and a run deep-links to the rules as they were.',
          'yes',
          'v1 baselines recorded live for both demo tenants; app and agent runs carry policy_version.',
        ],
      ],
      [
        'lawful-basis',
        'Lawful basis and purpose per data source',
        'Record why the organization may process each source, and what it may be used for.',
        '/data/domains',
        'Review sources without a basis',
        'NARROWED to a genuine human decision 2026-08-04. The vocabulary could not express "this source holds no personal data", so business records — a pricing rate card, a vendor list, the general ledger, branch data, competitor intel — could only be left permanently flagged or stamped with a fiction like "legitimate use". Added `not-personal-data` as an explicit DECLARATION (labelled as such, kept last, and excluded from isProcessingBasis so it can never be counted as a basis), and applied it to the 10 that hold none. On the real tenants exactly TWO sources still owe a basis — both "reimbursement quota", both employee-linked and graded confidential — which is the gate\'s own "a person has to decide those". Deliberately NOT auto-assigned: choosing between employment and consent for staff data is a DPO decision, and guessing it to green a surface is the failure this whole item guards against.',
        [
          'yes',
          'The release stores a DPDP lawful basis and purpose on each data domain.',
          'yes',
          'Runs resolve the basis from the domains their steps bound; an ungrounded source is never defaulted to consent.',
          'yes',
          'Settable in the domain form, shown on every card, and filterable to the gaps.',
          'partial',
          'Graded 25 live sources. 2026-08-04: sources holding no personal data now declare that explicitly rather than sitting in the same bucket as an unanswered question, taking the real-tenant gap from 8 to 2 — and those 2 are the employee-linked ones a DPO must decide. Still partial, correctly: an outstanding human decision is not a closed control, and the overview keeps reporting them.',
        ],
      ],
      [
        'access-review',
        'Access review that applies its decisions',
        'Certify who should still have access, and have the removals actually happen.',
        '/governance/access/review',
        'Open access review',
        '',
        [
          'yes',
          'The release records a durable, reasoned certification artefact per review.',
          'yes',
          'Each decision is executed and what actually happened is stored beside what was decided.',
          'yes',
          'A Console route ordered by risk, refusing an incomplete or unreasoned review.',
          'yes',
          'Driven through the real seams on a scratch org: 19/19 checks, including a failed application preserved on the record.',
        ],
      ],
      [
        'retention-evidence',
        'Retention that runs and leaves evidence',
        'Prove that records were deleted when the retention limit said they would be.',
        '/governance/evidence/retention',
        'Open retention evidence',
        'CLOSED on a real tenant 2026-08-04. Warehouse and lake purging still stays with the data engine and is reported as deferred, not claimed — that part is an ownership boundary, not a gap.',
        [
          'yes',
          'The release owns retention rules and a durable record of every sweep.',
          'yes',
          'The sweep executes over console-owned record classes and re-counts what is left afterwards.',
          'yes',
          'Rules and sweep evidence are a Console route under Evidence.',
          'yes',
          'Applied on BOTH real tenants 2026-08-04. Rules set per record class with defensible windows for the sector — bank 2555 days on app runs, insurer 3650 (life records run longer), 365 on agent runs, 1095 on indexed document text — then a sweep executed and filed on each. It deleted nothing, correctly and explicitly: the oldest data is 55 days old, so nothing was past its window, and `remaining` is RE-COUNTED after the work so the record proves the deletion rather than asserting it. The artefact reads "0 records past their retention limit removed · nothing past the limit remains" per class, which is the honest outcome rather than a reassuring one.',
        ],
      ],
      [
        'self-lifecycle',
        'Console process lifecycle',
        'Deploy, restart, and roll back the control plane safely.',
        '/operations/admin',
        'Inspect administration',
        'Keep process lifecycle in the private deployment runbook; expose immutable build and rollback evidence without adding an unsafe self-restart action.',
        [
          'yes',
          'The release has native start, stop, deploy, and rollback runbooks.',
          'partial',
          'Deployment scripts own lifecycle; the running process does not control itself.',
          'partial',
          'Administration explains host ownership but does not execute deployment lifecycle.',
          'yes',
          'The fleet ledger records an immutable release and prepared rollback source.',
        ],
      ],
    ],
  }),
  audit({
    serviceId: 'edge-gateway',
    serviceLabel: 'Network Gateway',
    upstreamVersion: 'Caddy v2.11.4; Coraza Caddy v2.5.0; caddy-ratelimit v0.1.0',
    versionSource:
      'onprem-fleet-orchestration deploy/onprem/edge-runtime-contract.json at bcd5b63; live binary SHA b3764397458fc383c803898b56d5144178dbd9e4742502f3361dff03cd128b5f; live config SHA a5a4b4c29eb214a13cd77d4d5cda00ad0cac4c00b256f9937944cb998b31dd0a',
    denominatorSource:
      'https://github.com/caddyserver/caddy/tree/v2.11.4; https://github.com/corazawaf/coraza-caddy/tree/v2.5.0; https://github.com/mholt/caddy-ratelimit/tree/v0.1.0',
    auditState: 'current',
    summary:
      'Bounded network-edge denominator: host/path proxying, WAF, per-client rate limiting, guarded file delivery, access evidence, and supervised recovery. Cloudflare owns public TLS termination.',
    capabilities: [
      [
        'http-routing',
        'Host and path routing',
        'Route public hosts and bounded API paths to their canonical on-prem owners.',
        '/operations/edge',
        'Inspect edge routes',
        'Route creation and mutation stay deployment-owned; the Console exposes live evidence, not an unsafe ad-hoc proxy editor.',
        [
          'yes', 'Caddy v2.11.4 provides host/path matching, reverse proxy, rewrite, and static responses.',
          'yes', 'The versioned Caddyfile owns the active gateway, AI, webhook, Console, and auxiliary routes.',
          'partial', 'The Edge overview exposes configured traffic and posture but does not mutate the deployment file.',
          'yes', 'Live root, API, file, and webhook routes reached their intended handlers.',
        ],
      ],
      [
        'waf',
        'Web application firewall',
        'Reject scanner, injection, traversal, and script payloads before application code.',
        '/operations/edge/waf',
        'Manage WAF intent',
        'The Console persists desired WAF intent but cannot yet validate and apply/reload it through the host-owned recovery contract.',
        [
          'yes', 'Coraza Caddy v2.5.0 and Coraza v3.7.0 are compiled into the pinned binary.',
          'yes', 'The versioned edge snippet executes Coraza before terminal route handlers.',
          'partial', 'WAF status and intent CRUD exist; applying that intent remains deployment-owned.',
          'yes', 'A live sqlmap user-agent request was rejected with HTTP 403.',
        ],
      ],
      [
        'rate-limiting',
        'Per-client rate limiting',
        'Bound abusive clients with route-specific sliding-window limits.',
        '/operations/edge/traffic',
        'Inspect traffic',
        'Rate policy is visible in traffic evidence but has no safe Console edit/apply workflow.',
        [
          'yes', 'caddy-ratelimit v0.1.0 provides keyed sliding-window limits and Retry-After responses.',
          'yes', 'Rate limiting is ordered after Coraza and before every terminal handle.',
          'partial', 'Operators can inspect traffic but cannot edit or publish rate zones.',
          'yes', 'The live verifier isolated a synthetic client key and observed HTTP 429 after the configured threshold.',
        ],
      ],
      [
        'guarded-file-delivery',
        'Policy-owned file delivery',
        'Serve stable file URLs without bypassing object visibility, tenant, or owner policy.',
        '/work/files',
        'Manage files',
        '',
        [
          'yes', 'Caddy supports path handling, rewrite, and reverse proxy composition.',
          'yes', 'Only /files/media/* is rewritten to the canonical Console file API; arbitrary buckets return 404.',
          'yes', 'The Files surface owns upload, visibility, access, and deletion.',
          'yes', 'Live proof returned 404 for an anonymous private object, 200 with service auth, and 200 for a public seed, then deleted the probe.',
        ],
      ],
      [
        'access-evidence',
        'Access and rejection evidence',
        'Inspect edge traffic and blocked requests from the active JSON access stream.',
        '/operations/edge/blocked-requests',
        'Inspect blocked requests',
        '',
        [
          'yes', 'Caddy v2.11.4 emits structured JSON access logs with rotation.',
          'yes', 'The deploy excludes the runtime log, preserving its inode while Caddy is running.',
          'yes', 'Traffic and Blocked requests consume the canonical edge log adapter.',
          'yes', 'The live verifier observed the current log path grow across allowed and rejected probes.',
        ],
      ],
      [
        'supervised-recovery',
        'Validated edge recovery',
        'Validate exact config and modules before restarting the supervised edge.',
        '/operations/health/metrics',
        'Inspect platform health',
        'Restart remains root-owned by design; expose immutable evidence rather than a browser-triggered privileged action.',
        [
          'yes', 'Caddy provides config validation and supervised lifecycle commands.',
          'yes', 'The fleet recovery contract checks version, binary/config hashes, modules, launchd, policy paths, and log growth.',
          'partial', 'Platform health reports readiness but does not perform privileged host lifecycle actions.',
          'yes', 'Candidate validation, launchd kickstart, and all fail-closed live checks passed on S1.',
        ],
      ],
    ],
  }),
  audit({
    serviceId: 'gateway',
    serviceLabel: 'AI Gateway',
    upstreamVersion: 'Aggregator gateway-aggregator.mjs (script sha256 afc7410e5583262c) under repo-owned LaunchDaemon co.getoffgridai.gateway-aggregator',
    versionSource:
      'Repo-owned launch artifact deploy/onprem/{gateway-aggregator.sh, co.getoffgridai.gateway-aggregator.plist, install-gateway-aggregator.sh}, installed to /Library/LaunchDaemons on S1 2026-07-24 (root:wheel). Verified live: launchd-managed (ppid 1), RunAtLoad+KeepAlive, :8800/v1/models → HTTP 401 (live + API-key enforced). Auto-restart PROVEN 2026-07-24 — killing the aggregator (pid 37325) had launchd relaunch it (pid 37871) with :8800 answering again. Script identity sha256 afc7410e5583262c; token sourced from /Users/admin/offgrid/secrets/gateway-aggregator.token (never in-repo).',
    denominatorSource:
      'packages/gateway/src/index.ts; packages/gateway/src/cluster/types.ts; packages/gateway/src/queue/types.ts; Console cloud-egress contract at c0d3e793: src/lib/egress-dlp.ts, src/lib/egress-dlp-run.ts, src/lib/egress-policy-store.ts, and src/app/api/v1/chat/stream/route.ts',
    auditState: 'current',
    summary:
      'Bounded first-party denominator: authenticated inference, model discovery, fleet routing, and request governance. Direct backend access is not part of the operator contract.',
    capabilities: [
      [
        'authenticated-inference',
        'Authenticated inference',
        'Run OpenAI-compatible inference through the governed on-prem door.',
        '/runtime/gateways',
        'Inspect gateways',
        '',
        [
          'yes',
          'The first-party gateway supports authenticated OpenAI-compatible inference.',
          'yes',
          'src/lib/gateway.ts and the inference adapter call the real gateway with service credentials.',
          'yes',
          'Runtime gateway and model surfaces expose the governed endpoint.',
          'yes',
          'The fleet gate proved a real completion and invalid-bearer rejection.',
        ],
      ],
      [
        'fleet-routing',
        'Fleet model routing',
        'Discover healthy models and route requests across active fleet nodes.',
        '/runtime/models',
        'Manage models',
        '',
        [
          'yes',
          'The gateway aggregator release provides pool membership, health, and routing.',
          'yes',
          'Gateway control and model-catalog adapters consume the real pool.',
          'yes',
          'Models, gateways, nodes, and clusters expose the routing relationship.',
          'yes',
          'The eight-node registry and Qwen/Gemma/Qwythos completions were verified.',
        ],
      ],
      [
        'request-governance',
        'Request policy and guardrail spine',
        'Apply tenancy, policy, guardrails, routing, and audit to inference requests.',
        '/governance/posture',
        'Inspect governance posture',
        'The deployed aggregator proves authentication, attribution, and routing, while policy and guardrails run in the surrounding Console spine. Correlate one allowed and one denied inference across tenant, policy, guard, gateway traffic, and audit evidence before counting the gateway contract as complete.',
        [
          'yes',
          'The first-party gateway release supports the Off Grid governance contract.',
          'partial',
          'Credential, routing, attribution, and traffic paths are in the aggregator; policy and guardrails are enforced by the surrounding Console spine rather than a deployed gateway policy plug-in.',
          'yes',
          'Governance and runtime surfaces expose effective controls and evidence.',
          'partial',
          'Governed denial and merged guard scans passed separately, but the retained evidence does not correlate both with the same gateway request and traffic record.',
        ],
      ],
      [
        'cloud-egress-dlp',
        'Cloud egress DLP',
        'Mask or block sensitive text at the final governed boundary before a chat request is sent to a cloud model.',
        '/governance/egress',
        'Manage cloud egress protection',
        'CORRECTED 2026-08-04 against the live ledger — the previous text ("agent and app model calls, cloud tools, and outbound sinks do not enter it") understated real coverage. Measured: 24 gateway.egress.dlp events, every one carrying a run id, of which 21 come from the AGENT path (sampled run_8efafec2 / run_e0949b61 / run_14e9b52a all resolve to agent_runs rows, status done, org_bharat, outcome redacted) and 3 from chat. Outbound sinks mask through maskTextForSend before the body crosses the boundary. WHAT REMAINS GENUINELY UNPROVEN is the APP model call on a cloud-permitted pipeline: 0 app-spawned agent runs carry a DLP event, because every app run on this tenant is forced on-prem by the pipeline routing rule pii-local, so there is no cloud egress to screen. That is the leash working rather than a hole, but it means the app cloud-egress seam has no live artefact. Prove it on a scratch pipeline that permits cloud egress — do NOT relax a live tenant PII routing rule to manufacture the evidence.',
        [
          'yes',
          'First-party Console release c5e8e01e1852da63a7094ca99745fb0830af7710 provides a default-on mask policy, strict block mode, fail-closed guardrail handling, and an explicit admin opt-out for cloud-bound text.',
          'partial',
          'The chat stream loads the tenant policy, scans every outbound text unit through the selected PII adapter, replaces payload.messages before forwardToCloud, refuses the call when screening fails, and records gateway.egress.dlp against the current orgId. The GOVERNED-RUN seam is also live and is the majority of the evidence: agentrun computes the egress-DLP verdict over query + governed sources from the same scans (no re-scan), refuses the run on a blocked verdict, applies input masking via applyPiiEscalation and substitutes the sanitized answer before provenance signing — 21 of the 24 retained gateway.egress.dlp decisions come from that path. Still partial because the APP model call on a cloud-permitted pipeline has no live artefact (see gap).',
          'yes',
          'The URL-driven /governance/egress surface and admin GET/PATCH route expose per-org ON/OFF, mask-versus-block policy, detector readiness, and recent decisions; disabling protection is an explicit audited admin action.',
          'yes',
          'Fleet-verified 2026-07-22 on S1 at c5e8e01e1852da63a7094ca99745fb0830af7710: two temporary org_bharat chat turns containing synthetic PAN ABCDE1234F and an email routed to compat:openai/gpt-4o-mini; the provider-produced answer contained PAN [REDACTED] and [REDACTED_EMAIL_ADDRESS_3], while the tenant panel retained two gateway.egress.dlp masked/redacted decisions. Evidence: docs/screenshots/capabilities/egress-dlp-cloud-response-c5e8e01e.png and egress-dlp-ledger-c5e8e01e-wide.png.',
        ],
      ],
    ],
  }),
  audit({
    serviceId: 'llm-guard',
    serviceLabel: 'Content guardrails',
    upstreamVersion:
      'LLM Guard / llm-guard-api 0.3.16 (tag 32b14a4a2fa398df8b77fd748ee4bd387a4ac5ce; upstream archived and unmaintained)',
    versionSource:
      'off-grid-ai/onprem-fleet-orchestration@bc74d828e02db7566b32191650cb58360f9178ae deploy/onprem/services-node-b.yml (mutable laiyer/llm-guard-api:0.3.16 image tag; live digest not retained), SERVICE_MAP.md, and SERVER_STATE.md; upstream v0.3.16 tag 32b14a4a2fa398df8b77fd748ee4bd387a4ac5ce',
    denominatorSource:
      'https://github.com/protectai/llm-guard/tree/32b14a4a2fa398df8b77fd748ee4bd387a4ac5ce/llm_guard_api; https://github.com/protectai/llm-guard/blob/32b14a4a2fa398df8b77fd748ee4bd387a4ac5ce/llm_guard/input_scanners/__init__.py; https://github.com/protectai/llm-guard/blob/32b14a4a2fa398df8b77fd748ee4bd387a4ac5ce/llm_guard/output_scanners/__init__.py; current upstream README archive warning',
    auditState: 'current',
    summary:
      'Relevant 0.3.16 denominator: prompt analysis/redaction, prompt classifiers, output safety/quality scanners, scanner policy, authenticated and bounded API execution, availability controls, and telemetry. Off Grid sharding is a separate first-party integration. The upstream project and its models are archived and no longer maintained, so this deployment needs a migration owner and compensating security controls.',
    capabilities: [
      [
        'prompt-sanitization',
        'Prompt sanitization and sensitive-data protection',
        'Detect and redact PII, secrets, credentials, and configured text patterns before a model sees them.',
        GUARDRAILS_TEST_ROUTE,
        'Test prompt protection',
        '',
        [
          'yes',
          'LLM Guard 0.3.16 exposes /analyze/prompt and input Anonymize, Regex, and Secrets scanners; the S1 shard has live email/secret redaction evidence.',
          'yes',
          'The content-guardrail port calls the authenticated S1 aggregator, normalizes the real verdict and sanitized prompt, and blocks when the required PII shard cannot answer.',
          'yes',
          'Guardrails Overview, Protections, Masking, Recognizers, Thresholds, and Test expose prompt protection and a live scan action.',
          'yes',
          'Fleet-proven live: governed agent run run_b542fcf7 (org_bharat) redacted the raw PAN/email BEFORE the model (pre-guard pii:redacted + guardrail-rules masked "My PAN is ABCDE1234F"→"****"), then the model answered only from the redacted input — the pre-model redaction + downstream inference are retained together on the signed run. (This path 500-crashed at persist until the guardrail NUL-sentinel fix — the reason durable redacting runs never completed.)',
        ],
      ],
      [
        'prompt-threat-scanning',
        'Prompt threat and content classification',
        'Detect injection, toxicity, gibberish, language, banned content, code, and request-size risks.',
        GUARDRAILS_PROTECTIONS_ROUTE,
        'Manage prompt protections',
        'The deployed g6 shard runs only PromptInjection, Toxicity, Gibberish, and Language from the 15-class 0.3.16 input denominator. CORRECTED 2026-08-04: the claim that its optional-shard degradation header is discarded by the Console adapter is WRONG — guardrail-provider parses x-offgrid-guard-degraded and x-offgrid-guard-answered, and checks.ts writes them into the retained check detail (10 agent runs already carry \'coverage degraded: unavailable classifiers; answered by pii\'). What is genuinely still missing is the 15-class denominator, and catalog entries for other scanners do not reconfigure the static shard.',
        [
          'yes',
          'The pinned input-scanner module exports 15 scanner classes, including injection, toxicity, content restrictions, language, code, regex, secrets, and token limits.',
          'partial',
          'The Off Grid aggregator fans out to a required PII shard and an optional g6 classifier shard; g6 statically enables four input classifiers and merged verdicts are normalized.',
          'partial',
          'Protections exposes ten scanner cards and pipeline/org scope, but several cards are not present in deployed YAML and an enable action does not change the upstream scanner set.',
          'partial',
          'Fleet recovery proved a merged injection/toxicity-classifier plus PII response, but not a seeded bank or insurance journey and not optional-shard degradation visibility.',
        ],
      ],
      [
        'output-safety-quality',
        'Output safety and quality scanning',
        'Evaluate and sanitize model output for bias, sensitive content, relevance, factual consistency, refusal, malicious URLs, language, format, and toxicity.',
        GUARDRAILS_PROTECTIONS_ROUTE,
        'Inspect output protections',
        'Output scanning is wired to /analyze/output and retained per run; extend the deployed output-scanner set (add e.g. MaliciousURLs/Bias) and capture a retained run where the output guard BLOCKS, to move the workflow gate to full.',
        [
          'yes',
          'LLM Guard 0.3.16 exposes /scan/output and /analyze/output and exports 22 output scanner classes/variants spanning safety, policy, and quality.',
          'yes',
          'The content-guardrail adapter scanOutput() posts to /analyze/output with the prompt context; checks.ts post-phase routes through it (NOT the input endpoint). Live-verified: a toxic answer returned is_valid:false with Toxicity tripped (Sensitive/Regex -1); the normalizer maps that real shape to a blocked/redacted verdict (contract test pinned).',
          'partial',
          'The catalog names PII, toxicity, bias, and banned-topic output outcomes, but it does not manage or display actual output scanner configuration and results.',
          'partial',
          'Governed runs retain a post-phase output-guard verdict (e.g. run_f7aa3cb5 has a retained post /analyze/output pii check); the deployed output-scanner set is limited (Sensitive/Regex/Toxicity) and a retained run where the output guard BLOCKS is not yet captured.',
        ],
      ],
      [
        'scanner-policy-lifecycle',
        'Scanner configuration and lifecycle',
        'Select scanners, tune thresholds and parameters, suppress scanners per request, and roll out policy safely.',
        GUARDRAILS_PROTECTIONS_ROUTE,
        'Manage protection policy',
        'Make deployed YAML or its replacement policy store the single lifecycle owner. In 0.3.16 scanner definitions are static configuration and requests only support scanners_suppress; the Console currently sends an unsupported scanners object, so catalog scope and custom India recognizers do not alter the stock service.',
        [
          'yes',
          'The 0.3.16 API loads ordered input/output scanners and parameters from CONFIG_FILE, supports lazy loading and per-request scanners_suppress, and has no scanner CRUD API.',
          'partial',
          'The fleet owns versioned classifier YAML and first-party shard merge policy, but the S1 PII configuration is not represented here and request-body scanner configuration is ignored upstream.',
          'partial',
          'Console rules, recognizers, thresholds, and pipeline scope are CRUD-capable product state, but they are not reconciled into the selected upstream 0.3.16 static configuration.',
          'no',
          'No retained workflow proves that changing a Console scanner rule changes the scanners or parameters executed by either shard.',
        ],
      ],
      [
        'api-auth-rate-limits',
        'Authenticated and bounded scan API',
        'Protect scan endpoints with bearer/basic authentication and configurable request limits.',
        '/governance/secrets',
        'Manage service credentials',
        'Expose guard service credential rotation and effective rate-limit posture without leaking secrets; retain valid/invalid credential and throttling evidence.',
        [
          'yes',
          'The 0.3.16 API supports HTTP bearer or basic auth and configurable per-client rate limiting.',
          'yes',
          'The Console and aggregator send bearer credentials from protected deployment configuration; shard tokens remain node-local.',
          'no',
          'The Console has no LLM-Guard-specific credential rotation or effective rate-limit management surface.',
          'partial',
          'Authenticated live scan evidence exists, but invalid-token, rotation, and throttling behavior are not retained as one governed workflow.',
        ],
      ],
      [
        'availability-failure-policy',
        'Availability, timeouts, and failure policy',
        'Probe liveness/readiness, bound scanner latency, and choose fail-fast or aggregate behavior.',
        GUARDRAILS_ROUTE,
        'Inspect guardrail posture',
        'Expose required versus optional shard readiness and degradation. CORRECTED 2026-08-04: an optional classifier outage is NOT hidden — a degraded scan is retained on the run (run_658c93b8, run_cc73dcf2, run_f9208926 on org_bharat all carry \'coverage degraded: unavailable classifiers; answered by pii\', one of them blocked). What remains true is that /healthz only proves the aggregator process, /readyz is not consumed, and per-shard readiness is absent from the normalized Console verdict.',
        [
          'yes',
          'LLM Guard 0.3.16 provides /healthz and /readyz, prompt/output timeouts, scan_fail_fast, lazy loading, and deterministic HTTP errors.',
          'partial',
          'The Console bounds requests and fails closed on adapter/required-shard failure; Off Grid adds required/optional shard policy, and optional degradation IS propagated — the provider parses the degraded/answered headers and the pure check mapper folds them into the retained verdict, so a run whose classifier shard was down says so instead of reading clean. Still partial only for readiness (/readyz) consumption result.',
          'partial',
          'Overview shows configured/reachable state, but not readiness, per-shard health, model load state, degradation, latency, or recovery actions.',
          'partial',
          'Fleet recovery proves merged service behavior and required-shard recovery mechanics; it does not retain a governed-run outage/recovery journey.',
        ],
      ],
      [
        'telemetry',
        'Scanner telemetry',
        'Observe scanner verdict counts, latency, errors, traces, and service saturation.',
        '/insights/ai/overview',
        'Inspect AI behavior',
        'Configure and attribute the 0.3.16 Prometheus/OTel signals, retain per-scanner and per-shard evidence, and connect them to actionable Console diagnostics. Console logs alone are not this upstream telemetry path.',
        [
          'yes',
          'LLM Guard 0.3.16 supports Prometheus or OTLP metrics, OTLP/console tracing, structured logs, and per-scanner validity counters.',
          'no',
          'No production adapter reads or attributes LLM Guard metrics/traces; the deployed classifier config defaults traces to console and has no retained telemetry pipeline proof.',
          'no',
          'AI behavior and platform health do not expose LLM Guard scanner metrics, shard saturation, or trace evidence.',
          'no',
          'No bank or insurance journey links a guardrail verdict to upstream scanner telemetry.',
        ],
      ],
    ],
  }),
  audit({
    serviceId: 'keycloak',
    serviceLabel: 'Identity & SSO',
    upstreamVersion: '26.0.7',
    versionSource:
      'deploy/docker-compose.yml (quay.io/keycloak/keycloak:26.0.7); live docker ps on S1 verified 2026-07-23 — container offgrid-console-keycloak-1 running the pinned 26.0.7 image',
    denominatorSource: 'https://www.keycloak.org/docs/26.0.7/server_admin/',
    auditState: 'current',
    summary:
      'Relevant denominator: human SSO, service accounts, tenant access, session/revocation controls, and identity lifecycle. Full realm administration is intentionally abstracted.',
    capabilities: [
      [
        'human-sso',
        'Human SSO and sessions',
        'Authenticate operators through the configured enterprise identity provider.',
        '/governance/access',
        'Manage access',
        'CLOSED 2026-08-04 — the whole lifecycle, not just authentication.',
        [
          'yes',
          'Keycloak 26.0.7 provides OIDC realms, clients, and user sessions.',
          'yes',
          'The production provider is exercised end to end against realm `offgrid`, client `offgrid-console`: token issue, userinfo, logout, and post-logout rejection of BOTH tokens.',
          'yes',
          'Access and team surfaces manage product membership and roles.',
          'yes',
          'Full lifecycle proven live 2026-08-04 against the deployed Keycloak: login issued access + refresh with expires_in 300s; userinfo 200; logout 204; the refresh token then returned invalid_grant "Session not active"; and the still-unexpired ACCESS token returned userinfo 401. That last step is the one that matters — revocation invalidates the access token too, not merely the refresh, so a logged-out session cannot keep reading for the remainder of its 5-minute window.',
        ],
      ],
      [
        'service-accounts',
        'Service-account grants',
        'Issue client-credential grants to internal service adapters without hard-coded bearer tokens.',
        '/governance/access',
        'Inspect service access',
        'Expose the bounded client lifecycle and revocation posture without recreating raw realm administration.',
        [
          'yes',
          'Keycloak supports confidential clients and client-credential grants.',
          'yes',
          'The service credential broker exchanges OpenBao-held client secrets for grants.',
          'partial',
          'Access surfaces show product access; full client lifecycle is not a dedicated UI.',
          'yes',
          'Five client-credential grants passed in the fleet recovery evidence.',
        ],
      ],
      [
        'federation-mfa',
        'Federation and MFA policy',
        'Federate workforce identity and require suitable authentication assurance.',
        '/governance/access',
        'Manage access',
        'Audit the configured identity-provider and authentication-flow contracts, then expose assurance and recovery state without recreating raw Keycloak administration.',
        [
          'yes',
          'Keycloak 26.0.7 supports identity brokering and configurable authentication flows.',
          'no',
          'No production federation/MFA adapter contract is evidenced.',
          'no',
          'The Console does not expose federation or MFA posture.',
          'no',
          'No seeded bank or insurance identity-assurance workflow is verified.',
        ],
      ],
    ],
  }),
  audit({
    serviceId: 'temporal',
    serviceLabel: 'Durable Workflows',
    upstreamVersion: 'Temporal Server 1.25.2 / UI 2.32.0',
    versionSource:
      'onprem-fleet-orchestration deploy/onprem/services-node-a.yml (tags temporalio/auto-setup:1.25.2, temporalio/ui:2.32.0). Live image digests locked 2026-07-20 in SERVER_STATE.md: auto-setup@sha256:b1edc1e20002d958c8182f2ae08dee877a125083683a627a44917683419ba6a8, ui@sha256:82bf98dbe005a831b6bc5dc12ccd7bffd606af2032dae4821ae133caaa943d3d.',
    denominatorSource: 'https://docs.temporal.io/',
    auditState: 'current',
    summary:
      'Relevant denominator: durable app/agent/chat dispatch, run lifecycle, schedules, retries, visibility, cancellation, and worker readiness.',
    capabilities: [
      [
        'durable-dispatch',
        'Durable app, agent, and chat dispatch',
        'Submit governed work to durable task queues and preserve run history.',
        '/operations/runs',
        'Inspect runs',
        '',
        [
          'yes',
          'Temporal 1.25.2 provides durable workflows, activities, histories, and task queues.',
          'yes',
          'App, agent, chat, schedule, and visibility adapters use Temporal clients and workers.',
          'yes',
          'Operations runs and app/agent run detail pages expose durable state.',
          'yes',
          'The fleet C4 gate retained a 202 response with workflowId for an eligible governed run, then correlated its completed output and actor evidence; the Temporal Server/UI image digests are now locked in SERVER_STATE.md (2026-07-20).',
        ],
      ],
      [
        'run-actions',
        'Run cancellation, retry, and replay',
        'Intervene in a failed or long-running workflow without losing audit history.',
        '/operations/runs',
        'Manage runs',
        '',
        [
          'yes',
          'Temporal supports termination, cancellation, reset/replay, and retry semantics.',
          'yes',
          'The comprehensive adapter covers the full matrix on durable app-run workflows: cancelWorkflow (cancel/terminate) + row reconciliation, resetWorkflow (reset/replay from the first workflow task), and rerun via submitAppRun; eligibility is the pure decideAppRunControl.',
          'yes',
          'The app Runs tab renders status-appropriate controls on every run via availableAppRunControls: cancel/terminate for in-flight runs, replay/re-run for terminal runs.',
          'yes',
          'Verified live on the fleet 2026-07-20 through POST /api/v1/admin/app-runs/{id}/workflow: terminate killed running workflows and reconciled the row to cancelled; reset replayed a done run (apprun_f339b0ee: done→running, re-executing from its first task); rerun dispatched a fresh run (apprun_f339b0ee→apprun_96e8d21d, 201); the pure guard returned 400 on a bad action and 409 on ineligible state (cancel-a-done, reset-in-flight).',
        ],
      ],
      [
        'worker-readiness',
        'Worker and task-queue readiness',
        'Know whether app, agent, and chat queues have compatible pollers before accepting work.',
        '/operations/services/temporal',
        'Inspect workers',
        '',
        [
          'yes',
          'Temporal exposes task-queue pollers and worker build/versioning primitives.',
          'yes',
          'adapters/worker-readiness.ts probes DescribeTaskQueue directly (pure shaper task-queue-readiness.ts), reporting per-queue poller identity/count/backlog — readiness is no longer inferred from run state.',
          'yes',
          'The temporal/app-worker/agent-worker/chat-worker detail pages render a live readiness panel (WorkerReadinessPanel) with per-queue poller identity, count, and backlog.',
          'yes',
          'Verified live on the fleet 2026-07-20: /api/v1/admin/workers/readiness returned offgrid-apps, offgrid-agents, and offgrid-chat each with a compatible poller (e.g. 44550@offgrid-s1); the console rendered "3/3 queues ready".',
        ],
      ],
    ],
  }),
  audit({
    serviceId: 'app-worker',
    serviceLabel: 'App Worker',
    upstreamVersion:
      'First-party source 21bd2f1ab450a531c031df6167dd3aee2a800971 / @temporalio/worker 1.20.2',
    versionSource:
      'Console git snapshot 21bd2f1ab450a531c031df6167dd3aee2a800971; package-lock.json; onprem-fleet-orchestration launch manifest 3e91d5d357e824dabfe6a97a3bc0015b897c37c6',
    denominatorSource:
      'scripts/app-worker.mts; src/lib/app-run-durable.ts; src/lib/adapters/apprun.ts; src/worker/app-run.workflow.ts; src/worker/app-run.activities.ts; src/lib/app-run.ts; src/lib/app-run-resume.ts; src/lib/app-run-store.ts',
    auditState: 'current',
    summary:
      'Pinned first-party denominator: worker artifact identity, queue readiness, governed step execution, durable human review, failure recovery, and output persistence. Code-path evidence is kept separate from live worker proof.',
    capabilities: [
      [
        'artifact-identity',
        'Immutable worker artifact identity',
        'Identify the exact app-worker source, runtime dependency, launch contract, and deployed artifact before assigning work.',
        '/operations/services/app-worker',
        'Inspect app worker',
        '',
        [
          'yes',
          'The first-party worker source, Temporal Worker SDK 1.20.2, and launch-manifest revision are pinned independently.',
          'yes',
          'Each worker registers with Temporal identity <pid>@<host>#<sha8> — the deployed release SHA (from the immutable deploy/.deployed-sha stamp or OFFGRID_RELEASE_SHA) resolved by the pure worker-artifact-identity module — binding the running poller to its exact deployed artifact.',
          'yes',
          'The app-worker service-detail readiness panel surfaces the artifact SHA badge parsed from each live poller identity.',
          'yes',
          'Verified live on the fleet 2026-07-20: after deploy of SHA 2b256bbc, DescribeTaskQueue reported the offgrid-apps/offgrid-agents/offgrid-chat pollers as <pid>@offgrid-s1#2b256bbc — the live pollers correlated to the exact deployed artifact stamped in deploy/.deployed-sha.',
        ],
      ],
      [
        'task-queue-readiness',
        'Task queue and poller readiness',
        'Confirm a compatible app worker is polling offgrid-apps before durable work is accepted.',
        '/operations/services/app-worker',
        'Inspect app worker',
        '',
        [
          'yes',
          'scripts/app-worker.mts creates a Temporal Worker on the configured queue and fails startup when required runtime configuration is invalid.',
          'yes',
          'adapters/worker-readiness.ts probes DescribeTaskQueue for offgrid-apps and reports the app-worker poller identity/count/backlog directly — not inferred from Temporal availability or an eventual run state.',
          'yes',
          'The app-worker service detail page renders the live offgrid-apps poller (identity, count, backlog) via WorkerReadinessPanel.',
          'yes',
          'Verified live on the fleet 2026-07-20: after a worker restart, offgrid-apps reported compatible poller 44550@offgrid-s1 through the deployed /api/v1/admin/workers/readiness endpoint and rendered "Ready" in the console.',
        ],
      ],
      [
        'governed-step-execution',
        'Governed step execution',
        'Execute app steps with tenant, actor, pipeline, policy, guardrail, routing, and provenance context intact.',
        '/solutions/apps',
        'Manage apps',
        '',
        [
          'yes',
          'The pinned worker implements read-data, connector, agent, guardrail, human, and output step contracts.',
          'yes',
          'executeStepActivity re-resolves the app and pipeline binding at execution time and reuses the production governed step executor with organization and actor context.',
          'yes',
          'App build, run, review, report, and operations-run routes expose the complete operator journey.',
          'yes',
          'Verified live on the fleet 2026-07-20: bhapp_fnol (org_bharat) ran on the app-worker durable path (workflowId apprun-bhapp_fnol-apprun_f339b0ee, resumedInline:false) executing connector, agent (policy+guardrail+PII-mask+grounding), human, and output steps under its bound pipeline; run apprun_f339b0ee completed done with the agent step attributed to child run run_356ec4a8.',
        ],
      ],
      [
        'human-pause-resume',
        'Durable human pause and resume',
        'Pause a multi-step app at an authority gate, preserve history, and resume or reject it through an audited decision.',
        '/solutions/reviews',
        'Open review queue',
        '',
        [
          'yes',
          'The workflow waits durably on a review signal and models approval, rejection, and edited output.',
          'yes',
          'The review API signals Temporal first, preserves paused state when the service is unreachable, and uses the inline resume adapter only when durable execution is not configured or not found.',
          'yes',
          'The review queue and run detail routes expose the pending decision and permitted action.',
          'yes',
          'Verified live on the fleet 2026-07-20 via durable Temporal signals (resumedInline:false): apprun_f339b0ee paused → approved → resumed → completed done; apprun_4440547c paused → rejected → halted to error; a repeat decision on a terminal run returned 409 (canReview guard). The approve-authority (unauthorized/under-threshold approver → 403) is covered by app-access-policy + app-access integration tests.',
        ],
      ],
      [
        'failure-recovery',
        'Failure and restart recovery',
        'Retry transient step failures and survive worker loss without duplicating side effects or losing the operator-visible run.',
        '/operations/runs',
        'Inspect durable runs',
        'Worker-loss recovery is proven live; worker build/version compatibility, an operator-visible safe-drain control, and a verified per-step side-effect idempotency contract are still outstanding.',
        [
          'yes',
          'Temporal histories, bounded activity retries, deterministic workflow IDs, and launchd KeepAlive provide the required recovery primitives.',
          'partial',
          'The workflow has retry policy and deterministic dispatch, but the worker lacks build-version compatibility, safe drain, and a verified side-effect idempotency contract for every step.',
          'partial',
          'Run detail exposes failure state and actions, but app-worker safe-drain and poller build-compatibility are not operator-visible.',
          'yes',
          'Verified live on the fleet 2026-07-21: paused run apprun_fe17e9f5 survived an app-worker kill+restart (remained awaiting_human across the worker-loss), was then approved via a durable signal (resumedInline:false), and the RESTARTED worker resumed and completed it (done) — an interrupted app-worker run recovered after process restart.',
        ],
      ],
      [
        'output-persistence',
        'Output and run-state persistence',
        'Persist step state, final output, report artifacts, and provenance so an operator can inspect and act on the result later.',
        '/operations/runs',
        'Inspect run outputs',
        'PARTLY CLOSED 2026-08-04: persistState no longer swallows database errors — it retries with bounded exponential backoff (total under a second, because an activity that retries for a minute stalls the run it is recording) and then logs APP_RUN_PERSIST_FAILED carrying run/org/status/attempts and the error CAUSE, at error level for a TERMINAL run (nothing later corrects it) and warn for a mid-run write (the next step supersedes it). The failure cannot go in the database we just failed to reach, so it goes to the worker log with the run id — without which the alarm is unactionable, since the repair is "re-persist THIS run". STILL OPEN: the automated replay/backfill itself, which needs a route that re-persists from authoritative Temporal state.',
        [
          'yes',
          'The app runtime defines app_runs persistence, aggregate output, signed report artifacts, and Temporal workflow history.',
          'partial',
          'The worker persists state around transitions and a transient write failure is now RETRIED rather than swallowed, with an unrecoverable one recorded loudly against its run id (5 unit tests cover the backoff bound, the run-id-carrying line, the Drizzle cause extraction, a non-Error throw, and the terminal-vs-mid-run distinction). Still partial because the automated replay/backfill from Temporal state does not exist — the operator is told which run to repair, not repaired for.',
          'yes',
          'App and operations run detail/report routes expose persisted status, output, and artifacts when writes succeed.',
          'yes',
          'Verified live on the fleet 2026-07-20: run apprun_f339b0ee completed done with all five steps persisted (including the output step) and its agent child run run_356ec4a8 carrying a signed ed25519 provenance — a worker-completed output correlated to its database row and provenance record.',
        ],
      ],
    ],
  }),
  audit({
    serviceId: 'agent-worker',
    serviceLabel: 'Agent Worker',
    upstreamVersion:
      'First-party source (shared worker binary) / @temporalio/worker 1.20.2',
    versionSource:
      'Console git snapshot; package-lock.json (@temporalio/worker 1.20.2); onprem-fleet-orchestration launch manifest (co.getoffgridai.agent-worker)',
    denominatorSource:
      'scripts/agent-worker.mts; src/lib/agentrun.ts; src/lib/adapters/worker-readiness.ts; src/worker/*.workflow.ts; src/worker/*.activities.ts',
    auditState: 'current',
    summary:
      'The agent worker is the durable poller for the offgrid-agents task queue — the execution spine for governed agent runs (tools, retrieval, guardrails, provenance). It shares the app-worker artifact-identity and readiness machinery, scoped to its own queue.',
    capabilities: [
      [
        'artifact-identity',
        'Immutable worker artifact identity',
        'Bind the running offgrid-agents poller to its exact deployed artifact before assigning work.',
        '/operations/services/agent-worker',
        'Inspect agent worker',
        '',
        [
          'yes',
          'The shared first-party worker source, Temporal Worker SDK 1.20.2, and launch-manifest revision are pinned independently.',
          'yes',
          'Each poller registers with Temporal identity <pid>@<host>#<sha8> resolved from the immutable deploy/.deployed-sha stamp via the pure worker-artifact-identity module.',
          'yes',
          'The agent-worker service-detail readiness panel surfaces the artifact SHA badge parsed from each live poller identity.',
          'yes',
          'Verified live on the fleet 2026-07-20: DescribeTaskQueue reported the offgrid-agents poller as <pid>@offgrid-s1#2b256bbc — the live agent poller correlated to the exact deployed artifact stamped in deploy/.deployed-sha.',
        ],
      ],
      [
        'task-queue-readiness',
        'Task queue and poller readiness',
        'Confirm a compatible agent worker is polling offgrid-agents before durable agent work is accepted.',
        '/operations/services/agent-worker',
        'Inspect agent worker',
        '',
        [
          'yes',
          'scripts/agent-worker.mts creates a Temporal Worker on offgrid-agents and fails startup when required runtime configuration is invalid.',
          'yes',
          'adapters/worker-readiness.ts probes DescribeTaskQueue for offgrid-agents and reports poller identity/count/backlog directly — not inferred from Temporal availability.',
          'yes',
          'The agent-worker service detail renders the live offgrid-agents poller (identity, count, backlog) via WorkerReadinessPanel.',
          'yes',
          'Verified live on the fleet 2026-07-20: offgrid-agents reported a compatible poller through the deployed /api/v1/admin/workers/readiness endpoint.',
        ],
      ],
      [
        'governed-agent-execution',
        'Governed agent-run execution',
        'Execute agent runs (tool calls, retrieval, guardrails, routing) with tenant, actor, policy, and provenance context intact.',
        '/solutions/agents',
        'Manage agents',
        '',
        [
          'yes',
          'The agent runtime executes governed tool/retrieval/guardrail steps under the entity-consumption hierarchy (agent → pipeline → gateway → model).',
          'yes',
          'agentrun.ts + the shared governed step executor re-resolve the agent + pipeline binding at execution time with organization and actor context.',
          'yes',
          'Agent build/detail/runs routes expose the full operator journey.',
          'yes',
          'Verified live on the fleet: a governed agent run executed on the durable path as a child run (e.g. app-worker run apprun_f339b0ee delegated its agent step to child run run_356ec4a8 on offgrid-agents), and this session an org-brain-tool agent run returned cited retrieval.',
        ],
      ],
      [
        'failure-recovery',
        'Failure and restart recovery',
        'Retry transient step failures and survive worker loss without duplicating side effects.',
        '/operations/runs',
        'Inspect durable runs',
        'Shares the app-worker recovery primitives; worker build/version compatibility, a safe-drain control, and a per-step side-effect idempotency contract are still outstanding for the agent queue.',
        [
          'yes',
          'Temporal histories, bounded activity retries, deterministic workflow IDs, and launchd KeepAlive provide the recovery primitives.',
          'partial',
          'Retry policy and deterministic dispatch exist, but build-version compatibility, safe drain, and a verified per-step idempotency contract are not complete.',
          'partial',
          'Run detail exposes failure state; agent-worker safe-drain and poller build-compatibility are not operator-visible.',
          'partial',
          'Worker-loss recovery is proven for the shared worker on offgrid-apps (2026-07-21); an equivalent interrupted-agent-run recovery is not separately retained.',
        ],
      ],
    ],
  }),
  audit({
    serviceId: 'chat-worker',
    serviceLabel: 'Chat Worker',
    upstreamVersion:
      'First-party source (shared worker binary) / @temporalio/worker 1.20.2',
    versionSource:
      'Console git snapshot; package-lock.json (@temporalio/worker 1.20.2); onprem-fleet-orchestration launch manifest (co.getoffgridai.chat-worker)',
    denominatorSource:
      'scripts/chat-worker.mts; src/app/api/v1/chat/stream/route.ts; src/lib/adapters/worker-readiness.ts',
    auditState: 'current',
    summary:
      'The chat worker is the durable poller for the offgrid-chat task queue. Its artifact identity and queue readiness are live-verified; end-to-end governed chat execution on the durable path is not separately re-proven in this audit and is kept honest as partial.',
    capabilities: [
      [
        'artifact-identity',
        'Immutable worker artifact identity',
        'Bind the running offgrid-chat poller to its exact deployed artifact before assigning work.',
        '/operations/services/chat-worker',
        'Inspect chat worker',
        '',
        [
          'yes',
          'The shared first-party worker source, Temporal Worker SDK 1.20.2, and launch-manifest revision are pinned independently.',
          'yes',
          'Each poller registers with Temporal identity <pid>@<host>#<sha8> resolved from the immutable deploy/.deployed-sha stamp via the pure worker-artifact-identity module.',
          'yes',
          'The chat-worker service-detail readiness panel surfaces the artifact SHA badge parsed from each live poller identity.',
          'yes',
          'Verified live on the fleet 2026-07-20: DescribeTaskQueue reported the offgrid-chat poller as <pid>@offgrid-s1#2b256bbc — the live chat poller correlated to the exact deployed artifact.',
        ],
      ],
      [
        'task-queue-readiness',
        'Task queue and poller readiness',
        'Confirm a compatible chat worker is polling offgrid-chat.',
        '/operations/services/chat-worker',
        'Inspect chat worker',
        '',
        [
          'yes',
          'The chat worker creates a Temporal Worker on offgrid-chat and fails startup on invalid runtime configuration.',
          'yes',
          'adapters/worker-readiness.ts probes DescribeTaskQueue for offgrid-chat and reports poller identity/count/backlog directly.',
          'yes',
          'The chat-worker service detail renders the live offgrid-chat poller via WorkerReadinessPanel.',
          'yes',
          'Verified live on the fleet 2026-07-20: offgrid-chat reported a compatible poller through the deployed /api/v1/admin/workers/readiness endpoint.',
        ],
      ],
      [
        'governed-chat-execution',
        'Governed chat execution',
        'Serve governed chat turns with tenant, actor, policy, guardrail, and egress-DLP context intact.',
        '/workspace/chat',
        'Open chat',
        'RE-VERIFIED 2026-08-04 on the durable path. Note for the next auditor: there is no chat_runs table — a chat run\'s durable record is its AUDIT event plus a signed provenance entry, so looking for a runs row and finding none reads as failure when the evidence is elsewhere. That cost a wrong conclusion here before the activity was read.',
        [
          'yes',
          'The chat stream route applies governance (guardrails, egress-DLP) over the entity-consumption hierarchy.',
          'yes',
          'The durable path is proven, not just registered: dispatchChatRun returned mode=durable with a Temporal workflow id (chatrun-conv_proof_…-chatrun_6b9b88d9), the offgrid-chat worker drained it, and recordChatRunGovernance persisted the governance record. Dispatch alone was not treated as proof — the drain was verified separately.',
          'yes',
          'The chat surface exposes the operator chat experience.',
          'yes',
          'Live evidence retained 2026-08-04: the durable run left audit event chat.run outcome=ok model=onprem/qwen3-vl-8b org=org_bharat keyed to run chatrun_6b9b88d9 — a governed chat turn recorded through the worker rather than inline.',
        ],
      ],
    ],
  }),
  audit({
    serviceId: 'landing',
    serviceLabel: 'Public Landing Site',
    upstreamVersion: 'Static marketing site on GitHub Pages (getoffgridai.co)',
    versionSource:
      'website repo (static export); live GET https://getoffgridai.co verified 2026-07-24 → HTTP 200, served by GitHub.com/Varnish',
    denominatorSource:
      'website repo static export; https://docs.github.com/en/pages (GitHub Pages hosting contract)',
    auditState: 'current',
    summary:
      'The public marketing landing site is deployed and reachable. Its capability from the operator plane is limited to public availability and edge delivery — it is a separate product surface, not a console-governed entity.',
    capabilities: [
      [
        'public-availability',
        'Public site availability',
        'Serve the public Off Grid AI marketing site to visitors.',
        '/operations/services/landing',
        'Inspect landing site',
        '',
        [
          'yes',
          'The marketing site is published as a static export on GitHub Pages.',
          'yes',
          'The getoffgridai.co apex DNS resolves to the GitHub Pages/Varnish edge that serves the export.',
          'yes',
          'The public landing site itself is the user-facing surface.',
          'yes',
          'Live-verified 2026-07-24: GET https://getoffgridai.co returned HTTP 200 with title "Off Grid - Run AI Locally on Your Phone" served by GitHub.com/Varnish.',
        ],
      ],
      [
        'edge-delivery',
        'Edge caching and TLS delivery',
        'Deliver the site over TLS through the hosting CDN edge.',
        '/operations/services/landing',
        'Inspect landing delivery',
        'Delivery is host-owned (GitHub Pages + Varnish); the console does not manage cache, headers, or certificates for this external surface.',
        [
          'yes',
          'GitHub Pages fronts content with Varnish caching and managed TLS.',
          'partial',
          'DNS points at the host, but the console owns no deploy/cache/cert adapter for this external site.',
          'no',
          'No console UI manages the landing-site edge.',
          'partial',
          'The site is delivered over TLS to visitors, but no console workflow operates its edge.',
        ],
      ],
    ],
  }),
  audit({
    serviceId: 'status-page',
    serviceLabel: 'Public Status Page',
    upstreamVersion: 'Next.js status surface behind Cloudflare (status.getoffgridai.co)',
    versionSource:
      'Console/status app; live GET https://status.getoffgridai.co verified 2026-07-24 → HTTP 200, x-powered-by:Next.js behind Cloudflare',
    denominatorSource:
      'src/app status surface; https://nextjs.org/docs (App Router hosting contract)',
    auditState: 'current',
    summary:
      'The public status page is deployed and reachable behind the Cloudflare tunnel. Its operator-plane capability is public reachability; live incident/uptime feeds beyond page availability are not audited here.',
    capabilities: [
      [
        'public-availability',
        'Public status availability',
        'Serve the public status surface to visitors.',
        '/operations/services/status-page',
        'Inspect status page',
        '',
        [
          'yes',
          'The status surface is a deployed Next.js app.',
          'yes',
          'status.getoffgridai.co is published through the Cloudflare tunnel to the deployed app.',
          'yes',
          'The public status page itself is the user-facing surface.',
          'yes',
          'Live-verified 2026-07-24: GET https://status.getoffgridai.co returned HTTP 200 (x-powered-by:Next.js, cf-cache-status:DYNAMIC) with title "Off Grid AI Console — AWS for AI".',
        ],
      ],
      [
        'status-feed',
        'Incident and uptime reporting',
        'Report component health, incidents, and historical uptime.',
        '/operations/services/status-page',
        'Inspect status feed',
        'Only page availability is verified; a live component-health/incident/uptime feed on this surface is not audited — add and verify the data feed before promoting these gates.',
        [
          'yes',
          'A status surface can render component health, incidents, and uptime history.',
          'partial',
          'The page is served, but a live health/incident data feed is not confirmed here.',
          'partial',
          'The page renders, but the incident/uptime feed is not audited.',
          'partial',
          'Page availability is proven; a live status feed is not retained as evidence.',
        ],
      ],
    ],
  }),
  audit({
    serviceId: 'cloudflared',
    serviceLabel: 'Cloudflare Tunnel',
    upstreamVersion: 'cloudflared tunnel (named tunnel 70f8a607, config-file run)',
    versionSource:
      'live `pgrep -fl cloudflared` on S1 verified 2026-07-24 → PID 90637 running `cloudflared tunnel --config ~/.cloudflared/config.yml run 70f8a607-…`; co.getoffgridai.tunnel LaunchAgent',
    denominatorSource: 'https://developers.cloudflare.com/cloudflare-one/connections/connect-networks/',
    auditState: 'current',
    summary:
      'Cloudflared publishes the on-prem console and SSH ingress to the outside world through a named tunnel. It is proven live (this session reached the fleet through it); its config is host-owned and the console does not manage the tunnel.',
    capabilities: [
      [
        'ingress-tunnel',
        'Outbound-only ingress tunnel',
        'Publish selected on-prem services to public hostnames over an outbound-only tunnel.',
        '/operations/services/cloudflared',
        'Inspect tunnel',
        '',
        [
          'yes',
          'cloudflared establishes an outbound-only tunnel that publishes configured hostnames without inbound ports.',
          'yes',
          'The fleet runs a named tunnel (70f8a607) under the co.getoffgridai.tunnel LaunchAgent with a host-owned config.',
          'yes',
          'The service detail exposes tunnel readiness.',
          'yes',
          'Verified live 2026-07-24: this session reached S1 (docker, console, SSH) through the running cloudflared tunnel (PID 90637) — end-to-end ingress proven.',
        ],
      ],
      [
        'tunnel-config-management',
        'Tunnel route and credential management',
        'Manage ingress routes, credentials, and high-availability replicas for the tunnel.',
        '/operations/services/cloudflared',
        'Inspect tunnel config',
        'Tunnel routes/credentials are host-owned (~/.cloudflared/config.yml); the console has no route/credential/HA management surface, and single-replica has no failover.',
        [
          'yes',
          'cloudflared supports ingress rules, credential rotation, and replica-based HA.',
          'partial',
          'The config file is host-owned; no console adapter manages routes or credentials.',
          'no',
          'No console UI manages tunnel routes.',
          'partial',
          'A single replica serves ingress; no HA/failover is configured or proven.',
        ],
      ],
    ],
  }),
  audit({
    serviceId: 'gateway-control',
    serviceLabel: 'Gateway Control Plane',
    upstreamVersion: 'First-party gateway aggregator control API (S1:8800)',
    versionSource:
      'scripts/gateway-aggregator.mjs; live probe on S1 verified 2026-07-24 → 127.0.0.1:8800/v1/models returned HTTP 401 (endpoint live, API-key enforced)',
    denominatorSource: 'packages/gateway/src/cluster/types.ts; packages/gateway control-plane contract',
    auditState: 'current',
    summary:
      'The gateway control plane exposes the model registry and routing configuration behind the aggregator. Its control endpoint is live and API-key enforced; a full console-driven registry-management round-trip is not re-proven this cycle.',
    capabilities: [
      [
        'model-registry-control',
        'Model registry and routing control',
        'Expose the authorized model registry and routing configuration to governed consumers.',
        '/runtime/models/routing',
        'Manage routing',
        'The control endpoint is live and secured; a console-driven registry mutation round-trip (add/disable a model through the control API with the gateway key) is not re-proven in this audit.',
        [
          'yes',
          'The gateway aggregator exposes a control API for model discovery and routing policy.',
          'partial',
          'The console reads authorized models through the gateway; a control-plane mutation adapter is not proven here.',
          'yes',
          'Runtime → Models → Routing surfaces the model/routing configuration.',
          'partial',
          'Verified live 2026-07-24: 127.0.0.1:8800/v1/models returned HTTP 401 (endpoint live + API-key enforced); a management mutation with the gateway key is not retained as evidence.',
        ],
      ],
    ],
  }),
  audit({
    serviceId: 'litellm-forwarder',
    serviceLabel: 'LiteLLM Forwarder',
    upstreamVersion: 'S1→g5 TCP forward to the LiteLLM proxy (:4000)',
    versionSource:
      'co.getoffgridai.tcp-forward / onyx-llm-bridge LaunchAgents; live probe on S1 verified 2026-07-24 → 127.0.0.1:4000/health returned HTTP 401 (LiteLLM reachable through the forward)',
    denominatorSource:
      '../onprem-fleet-orchestration/deploy/onprem/SERVICE_MAP.md (S1→g5 forwarders); host forward contract',
    auditState: 'current',
    summary:
      'The forwarder bridges S1 loopback to the g5 LiteLLM proxy so the gateway and console reach model inference across the LAN boundary. Traffic flow is proven live; the legacy root forward plists are not fully repo-owned (a disclosed reproducibility gap).',
    capabilities: [
      [
        'llm-transport',
        'LiteLLM transport forward',
        'Carry inference traffic from S1 to the g5 LiteLLM proxy.',
        '/operations/services/litellm-forwarder',
        'Inspect forwarder',
        'The forward carries live traffic, but the legacy root S1→g5 forward plists are not yet repo-owned — an explicit recovery/reproducibility gap recorded in the fleet ledger.',
        [
          'yes',
          'A TCP forward maps an S1 loopback port to the g5 LiteLLM endpoint.',
          'yes',
          'The console/gateway reach LiteLLM through the S1 loopback the forward publishes.',
          'partial',
          'The forward is host/LaunchAgent-owned; no console UI manages it.',
          'yes',
          'Verified live 2026-07-24: 127.0.0.1:4000/health returned HTTP 401 (LiteLLM live and reachable through the forward).',
        ],
      ],
    ],
  }),
  audit({
    serviceId: 'observability-forwarder',
    serviceLabel: 'Observability Forwarder',
    upstreamVersion: 'S1→g5/g6 TCP forwards to the observability backends',
    versionSource:
      'co.getoffgridai.tcp-forward LaunchAgent + S1→g6 loopback forwards; live probe on S1 verified 2026-07-24 (sibling forwards serving — Langfuse loopback 8931 group)',
    denominatorSource:
      '../onprem-fleet-orchestration/deploy/onprem/SERVICE_MAP.md (S1 loopback forwards); host forward contract',
    auditState: 'current',
    summary:
      'The forwarder bridges S1 loopback to the observability backends (Langfuse/OTel/metrics) so the console reads telemetry across the LAN boundary. The forward mechanism is proven live via sibling forwards; a dedicated observability round-trip through this forward is not separately retained.',
    capabilities: [
      [
        'observability-transport',
        'Observability transport forward',
        'Carry telemetry read traffic from S1 to the observability backends.',
        '/operations/services/observability-forwarder',
        'Inspect forwarder',
        'CLOSED 2026-08-04, and the repo-ownership gap turned out to matter far more than reproducibility: this forward was DEAD. co.getoffgridai.obs-fwd.plist existed on the host but not in the repo, running gost as root against a hard-coded 192.168.1.65 — g5 had moved to .29 — so 127.0.0.1:9428 (VictoriaLogs) and :16686 (Jaeger) both returned 000 while offgrid-g5.local answered 200 on each. The ports WERE bound, so nothing detected it: a listener is not a route. Adopted into the repo-owned tcp-forward.js, which resolves .local per connection precisely so a DHCP lease cannot break a forward; the legacy root daemon is booted out.',
        [
          'yes',
          'A TCP forward maps S1 loopback ports to the observability backends.',
          'yes',
          'Console observability reads reach their backends through the S1 loopback forwards.',
          'partial',
          'Repo-owned and reproducible now (deploy/onprem/tcp-forward.js, recorded in SERVER_STATE.md); still no console UI manages forwards, which is deliberate — a forward is host plumbing.',
          'yes',
          'Round-trip retained 2026-08-04 through THIS forward: after the cutover 127.0.0.1:16686/api/services returned 5 services including offgrid-console, and a trace searched by its run_id tag resolved 2 traces carrying run_id + org. VictoriaLogs answers on :9428 and is genuinely EMPTY (hits: []) — reported as such rather than as a transport failure, which is the distinction the dead forward had been hiding.',
        ],
      ],
    ],
  }),
  audit({
    serviceId: 'fleet-forwarder',
    serviceLabel: 'Fleet Data Forwarder',
    upstreamVersion: 'S1→g6 loopback forwards to the data-plane services (:894x/:893x)',
    versionSource:
      'S1 loopback forwards (127.0.0.1:8941 warehouse, 8942 airbyte, 8944 GX, 8932 unleash, 8933 superset); live probe on S1 verified 2026-07-24',
    denominatorSource:
      '../onprem-fleet-orchestration/deploy/onprem/SERVICE_MAP.md (S1 127.0.0.1:894x→g6 map); host forward contract',
    auditState: 'current',
    summary:
      'The forwarder fronts the g6 data-plane services on S1 loopback ports so the console reaches the warehouse, ETL, quality, and BI engines across the LAN. Multiple forwards are proven serving live.',
    capabilities: [
      [
        'data-plane-transport',
        'Data-plane transport forward',
        'Carry console traffic from S1 loopback to the g6 data-plane services.',
        '/operations/services/fleet-forwarder',
        'Inspect forwarder',
        'Forwards serve live; they are host/LaunchAgent-owned with no console management surface, and share the fleet forward repo-ownership gap.',
        [
          'yes',
          'TCP forwards map S1 loopback ports to the g6 warehouse/ETL/quality/BI endpoints.',
          'yes',
          'The console reaches the g6 data-plane services through these S1 loopbacks (e.g. warehouse queries).',
          'partial',
          'The forwards are host/LaunchAgent-owned; no console UI manages them.',
          'yes',
          'Verified live 2026-07-24: S1 loopback forwards returned live responses — 8941 (warehouse) 200, 8944 (GX) 200, 8932 (unleash) 200, 8933 (superset) 302.',
        ],
      ],
    ],
  }),
  audit({
    serviceId: 'opa',
    serviceLabel: 'Policy Engine',
    upstreamVersion: '0.70.0',
    versionSource:
      'onprem-fleet-orchestration deploy/onprem/services-node-a.yml (configured tag openpolicyagent/opa:0.70.0); live `opa version` in container offgrid-services-a-opa-1 on S1 verified 2026-07-23 → Version 0.70.0, Build Commit 2ea031ea04e6a8afbc5dd22f656131dc3cfc5a7d',
    denominatorSource: 'https://www.openpolicyagent.org/docs/v0.70.0/',
    auditState: 'current',
    summary:
      'Relevant denominator: policy/module/data lifecycle, decision evaluation, bundles, decision logs, and failure posture. Rego stays behind governed product modules.',
    capabilities: [
      [
        'policy-decisions',
        'Governed policy decisions',
        'Evaluate tenant-scoped allow/deny decisions and preserve explainable evidence.',
        '/governance/policies/decisions',
        'Inspect decisions',
        '',
        [
          'yes',
          'OPA 0.70.0 provides policy evaluation and decision APIs.',
          'yes',
          'OFFGRID_ADAPTER_POLICY=opa is the selected adapter on both the console and the agent-worker (retained in the fleet .env.local); decisions are attributed engine:opa. First-party ABAC remains only as an unreachable-OPA safety fallback.',
          'yes',
          'Policies and evidence routes expose rules and decisions.',
          'yes',
          'Live: the offgrid/authz Rego was deployed via the console (HTTP 201, OPA-compiled); operator→allow and viewer→deny via /api/v1/admin/abac/evaluate both returned engine:opa; governed agent run run_9733f803 (Succeeded) recorded its policy step as label opa — "allow — OPA decision (offgrid/authz): true" (docs/screenshots/capabilities/policy-opa-run-attribution.png). Attribution is to OPA, not the ABAC fallback.',
        ],
      ],
      [
        'policy-lifecycle',
        'Policy and module lifecycle',
        'Create, edit, publish, version, and retire governed policy rules.',
        '/governance/policies/rules',
        'Manage rules',
        '',
        [
          'yes',
          'OPA provides policy and data document APIs.',
          'yes',
          'The console policy-module route deploys/compiles/publishes Rego to the live OPA (deployModule → PUT /v1/policies/{id}); invalid Rego is rejected at the gate before it can land.',
          'yes',
          'Rules, templates, modules, and decisions have canonical nested routes.',
          'yes',
          'Live: offgrid/authz was created + compiled + published (HTTP 201) and immediately changed OPA decisions (reload); an invalid Rego was rejected (HTTP 400 rego_parse_error "2:24 unexpected : token") and never landed in OPA (404 in storage); redeploy is idempotent (rollback = republish the prior module).',
        ],
      ],
      [
        'bundles-decision-logs',
        'Bundles and decision-log export',
        'Distribute policy safely and export complete decision telemetry.',
        '/governance/policies/decision-logs',
        'Inspect decision logs',
        'End-to-end OPA-pushed decision logs need the OPA decision_logs plugin pointed at the console ingest endpoint (a deploy-config step, not a console change); live bundles need OPA started with a bundle config. Both are surfaced honestly today.',
        [
          'yes',
          'OPA 0.70.0 supports bundles and decision-log plugins.',
          'yes',
          'A governed decision-log SINK (opa-audit adapter + self-migrating opa_decision_logs store) reads OPA /v1/config,/status,/policies and ingests + persists OPA decision-log uploads; bundle status is read from OPA. Verified live against OPA 0.70.0 on the box.',
          'yes',
          'Governance → Policies → Decision logs is a list→detail ledger (filters, aggregate, purge) + an honest read-only bundles view.',
          'partial',
          'Fleet-proven live: a decision event ingested → persisted → listed + aggregated (deny, engine:opa); the deployed OPA has decision_logs + bundles UNconfigured, so a real OPA-pushed log needs that deploy-config flip.',
        ],
      ],
    ],
  }),
  audit({
    serviceId: 'openbao',
    serviceLabel: 'Secrets Vault',
    upstreamVersion: '2.1.0',
    versionSource:
      'deploy/docker-compose.yml (openbao/openbao:2.1.0); live GET /v1/sys/health on S1:8200 verified 2026-07-23 → version 2.1.0, sealed:false',
    denominatorSource: 'https://openbao.org/docs/2.1.x/',
    auditState: 'current',
    summary:
      'Relevant denominator: secret CRUD, connector and service credentials, leases, mounts, rotation, auth, policy, audit, and recovery. Root administration is excluded.',
    capabilities: [
      [
        'secret-lifecycle',
        'Tenant secret lifecycle',
        'Create, read, update, and delete governed secrets without revealing values in client state.',
        '/governance/secrets/keys',
        'Manage keys',
        '',
        [
          'yes',
          'OpenBao 2.1.0 provides versioned KV secret lifecycle APIs.',
          'yes',
          'The secrets adapter and connector-secret seam perform server-side lifecycle operations.',
          'yes',
          'Keys and connector forms expose product-owned secret operations.',
          'yes',
          'Full CRUD proven live through the console routes against the unsealed OpenBao 2.1.0 (KV v2): WRITE (201) → LIST (key present, value NEVER in the JSON) → UPDATE → version metadata shows v1+v2 → soft-DELETE (v2 deletionTime set, v1 retained) — all tenant-scoped to org_bharat/ and recorded as secret.write rows in audit_events_v2. Screenshot: docs/screenshots/capabilities/secrets-lifecycle-keys.png.',
        ],
      ],
      [
        'dynamic-credentials',
        'Dynamic database credentials and leases',
        'Issue short-lived credentials, inspect leases, and revoke access.',
        '/governance/secrets/dynamic-database',
        'Manage dynamic credentials',
        '',
        [
          'yes',
          'OpenBao 2.1.0 provides database secrets and lease APIs.',
          'yes',
          'The database secrets engine is enabled at database/ against the console Postgres with a scoped read-only role (demo-readonly, default_ttl 2m/max 10m); the console dynamic-db route mints on-demand creds and the leases route lists them. Replayable via fleet setup-openbao-dynamic-db.sh.',
          'yes',
          'Dynamic database and lease routes are deep-linkable; the Dynamic-database view lists roles with a Generate-creds action (screenshot secrets-dynamic-db-role.png).',
          'yes',
          'Fleet-proven live end-to-end: MINT via the console governed route returned a real short-lived Postgres user (v-root-demo-rea-…, lease database/creds/demo-readonly/…); the cred AUTHENTICATED to Postgres (AUTH_OK, can_read=true); least-privilege held (DDL → "permission denied for schema public"); REVOKE (HTTP 204) then the same cred FATAL-failed to connect (role dropped). Issue→auth→least-privilege→revoke→teardown all observed.',
        ],
      ],
      [
        'vault-recovery',
        'Vault audit and disaster recovery',
        'Back up encrypted state, inspect audit devices, and recover without credential loss.',
        '/operations/backups',
        'Manage backups',
        'Backup → restore → unseal → verify + audit-device drill proven live and recorded (fleet vault-recovery-drill.sh). Next: surface the drill/backup record in the /operations/backups UI.',
        [
          'yes',
          'OpenBao supports file-backed storage, audit devices, and operational recovery procedures.',
          'yes',
          'A replayable drill (fleet vault-recovery-drill.sh) backs up the encrypted file-storage volume and restores it into a fresh vault; a file audit device is enabled and records requests with HMAC-obscured values.',
          'partial',
          'The /operations/backups surface lists backups but does not yet render this OpenBao restore drill as a first-class record.',
          'yes',
          'Fleet-proven live (non-destructive): a canary was written to the live vault; the encrypted data volume was tarred (29K, keyring included) and restored into a throwaway vault on :8201 (initialized:true/sealed:true); the SAME Shamir key unsealed it (sealed:false — no credential loss); the canary read back MATCHED (encryption keyring intact); a file audit device logged the read with the secret value HMAC-hashed, not plaintext. Throwaway + canary cleaned up.',
        ],
      ],
    ],
  }),
  audit({
    serviceId: 'unleash',
    serviceLabel: 'Feature Flags',
    upstreamVersion: 'unleash-server 6.6 — image sha256:790ab9540704 (unleashorg/unleash-server:6.6)',
    versionSource:
      'deploy/docker-compose.yml (unleashorg/unleash-server:6.6); live `docker inspect` of container offgrid-services-b-unleash-1 on g6 verified 2026-07-24 → image sha256:790ab9540704d4641864facac1f315a690b4e038b37dd1c0b717f7c1a7fdba25',
    denominatorSource: 'https://docs.getunleash.io/reference',
    auditState: 'current',
    summary:
      'Relevant denominator: flag CRUD, enablement, rollout strategies, variants, environment state, and audited product use. The mutable tag prevents a current availability claim.',
    capabilities: [
      [
        'flag-lifecycle',
        'Feature-flag lifecycle',
        'Create, describe, enable, disable, and archive product flags.',
        '/operations/configuration',
        'Manage configuration',
        'Pin the live image and prove create-to-archive through the operator UI.',
        [
          'yes',
          'Unleash 6.6 documentation describes feature CRUD and environment enablement.',
          'yes',
          'Admin adapters implement create, description update, toggle, and archive.',
          'partial',
          'Configuration consumes flags; complete lifecycle discoverability is not live-proven.',
          'no',
          'No seeded end-to-end flag lifecycle is recorded.',
        ],
      ],
      [
        'progressive-rollout',
        'Progressive rollout and variants',
        'Roll out a capability gradually and choose controlled variants.',
        '/operations/configuration',
        'Manage rollouts',
        'Prove a tenant-scoped percentage rollout and variant changes real product behavior.',
        [
          'yes',
          'Unleash 6.6 provides strategies and variants.',
          'yes',
          'Adapters implement rollout percentages and variant payloads.',
          'partial',
          'No dedicated rollout/variant journey is evidenced in the Console.',
          'no',
          'No bank or insurance workflow is verified under a rollout.',
        ],
      ],
    ],
  }),
  audit({
    serviceId: 'redis',
    serviceLabel: 'Redis',
    upstreamVersion: '7.4.9 (redis:7.4-alpine)',
    versionSource:
      'deploy/docker-compose.yml (redis:7.4-alpine); live `redis-cli INFO server` in container offgrid-services-b-redis-1 on g6 verified 2026-07-23 → redis_version:7.4.9',
    denominatorSource: 'https://redis.io/docs/latest/develop/',
    auditState: 'current',
    summary:
      'Relevant denominator: shared response-cache get/set/expiry/invalidation, backend health, and graceful fallback. Redis remains an implementation behind the cache port.',
    capabilities: [
      [
        'shared-response-cache',
        'Shared response cache',
        'Cache governed responses across Console processes with bounded expiry.',
        '/operations/services/redis',
        'Inspect Redis',
        'CLOSED 2026-08-04. Rendering the counters found the cache was NOT shared on this deployment: OFFGRID_REDIS_URL pointed at a forwarder on :8937 whose target host name (offgrid-g6.local) had stopped resolving from S1 — the node was healthy the whole time; the office has two WiFi networks and the nodes hop between them, so a name and a pinned IP both go stale, so every read and write silently fell back to per-process memory while the hit rate looked perfect. Redis now runs on S1 (compose profile "caching", :6379), the console points straight at it, and a separate client read back an entry the adapter wrote (TTL live) — cross-process sharing proven, not inferred.',
        [
          'yes',
          'Redis 7.4 provides key/value storage and expiry.',
          'yes',
          'Selection proven live, not inferred: a value written through redisCache was read STRAIGHT OUT OF REDIS by a separate client, so the shared store is genuinely serving rather than an in-process map wearing Redis\'s name. The adapter now also tallies WHICH BACKEND ANSWERED (cacheCounters), which is the only way the silent-degradation case is visible at all.',
          'yes',
          'The cache page (/runtime/models/cache) renders the counters as a distinct panel from the gateway cache, leading with the STATE because a degraded cache still posts a fine hit rate. Every process publishes its own snapshot and the panel sums them while keeping the split visible — a first version read only the rendering process, which serves pages and runs no inference, so it reported an idle cache while the workers used it. Stale snapshots are counted but marked; a failed read renders PARTIAL VIEW, never "nobody used the cache".',
          'yes',
          'Live on the deployment 2026-08-04: shared store up -> sharedHits 1, sharedWrites 1, misses 1, verdict "shared" ("50% of 2 reads hit"); a 1-second TTL lapsed -> the read returned null and was counted a MISS not a hit; shared store unreachable -> fallbackHits 1, fallbackWrites 1, verdict "degraded" with the value still served. Hit, miss, expiry and the degradation transition are all evidenced.',
        ],
      ],
      [
        'failure-fallback',
        'Cache failure fallback',
        'Continue safely on an in-process cache when Redis is unavailable.',
        '/operations/health',
        'Inspect health',
        'CLOSED 2026-08-04 for the failover behaviour and its honesty. The remaining nuance is history: the counters are per-process and reset on restart, so "when did it degrade" is not answerable across a redeploy — that needs retention, not a new mechanism.',
        [
          'yes',
          'The Off Grid cache contract defines an in-process fallback.',
          'yes',
          'The Redis adapter degrades to the in-process implementation AND records it. ANY fallback traffic makes the verdict degraded rather than being averaged into a healthy hit rate — one silently-local write is how "shared" stops being true, so it is not smoothed away.',
          'yes',
          'The verdict names the state and refuses both wrong readings: "Requests are still served, so nothing looks broken — but the cache is not shared, and two processes will disagree." An unexercised cache reports IDLE, never healthy.',
          'yes',
          'Failover drilled live 2026-08-04 in a fresh process against a dead endpoint (127.0.0.1:65533): health() false, the write and read both fell back, the value was STILL SERVED, and the verdict was degraded — proving continuity without hiding that the cache had stopped being shared. NOTE on method: a first attempt re-imported the module with a changed env and wrongly reported "shared", because REDIS_URL is captured at module load; only a fresh process exercises this path.',
        ],
      ],
    ],
  }),
  audit({
    serviceId: 'superset',
    serviceLabel: 'BI & Dashboards',
    upstreamVersion: '4.1.1',
    versionSource:
      'deploy/docker-compose.yml (apache/superset:4.1.1); live docker ps on g6 verified 2026-07-23 — container offgrid-services-b-superset-1 running the pinned 4.1.1 image',
    denominatorSource: 'https://superset.apache.org/docs/6.0.0/intro',
    auditState: 'current',
    summary:
      'Relevant operator denominator: governed dashboard provisioning, native chart read-back, embedded access, and bounded authoring. Superset remains the query engine; the Console owns the operator experience.',
    capabilities: [
      [
        'dashboard-provisioning',
        'Governed dashboard provisioning',
        'Idempotently create the Off Grid database, dataset, charts, and dashboard.',
        '/insights/usage/dashboards',
        'Manage dashboards',
        '',
        [
          'yes',
          'Superset 4.1.1 provides database, dataset, chart, dashboard, and embedded-config APIs.',
          'yes',
          'src/lib/superset.ts authenticates and performs idempotent find-or-create provisioning.',
          'yes',
          'Insights > Usage > Dashboards is the canonical governed dashboard route.',
          'yes',
          'The fleet ledger records dashboard 1 and charts 1 and 2 provisioned through the Console API.',
        ],
      ],
      [
        'native-chart-readback',
        'Native governed chart read-back',
        'Run Superset chart queries and render the results inside the Off Grid visual system.',
        '/insights/usage/dashboards',
        'View dashboards',
        '',
        [
          'yes',
          'Superset 4.1.1 provides chart-data APIs.',
          'yes',
          'src/lib/superset-data.ts resolves owned charts, runs chart-data queries, and shapes results.',
          'yes',
          'The Console renders the governed dashboard in its native Insights surface.',
          'yes',
          'The provisioned request and token charts query the live Console database.',
        ],
      ],
      [
        'embedded-access',
        'Scoped embedded dashboard access',
        'Mint short-lived guest access only after verifying the owned dashboard exists.',
        '/insights/usage/dashboards',
        'Open governed dashboard',
        'INVESTIGATED 2026-08-04 and the blocker is CONFIG DRIFT, not missing code. mintGuestToken correctly refuses to mint until the dashboard exists — it looks the dashboard up by OFFGRID_SUPERSET_EMBED_UUID (8cf450b7-3b71-47e8-8c2b-f86bc2a62b45) and that UUID matches no dashboard in the deployed Superset, while provisionDashboard reports a real dashboard at id 1 with a different uuid. So the two disagree about "provisioned", the surface shows a provisioning CTA instead of a blank iframe (which is the RIGHT failure), and no token can be minted to capture. Fixing it means pointing the env var at the intended dashboard\'s real UUID — a deployment decision about WHICH dashboard is the governed embed, not a code change, and guessing it would be inventing the answer. The refusal path itself is verified: configured=true, state=not-provisioned, no token issued.',
        [
          'yes',
          'Superset 4.1.1 supports embedded dashboard configuration and guest tokens.',
          'yes',
          'The Superset adapter verifies the dashboard before minting a scoped guest token.',
          'yes',
          'The dashboard route can open the governed embedded resource.',
          'partial',
          'Dashboard provisioning is proven, but guest-token expiry and revocation are not in fleet evidence.',
        ],
      ],
      [
        'sql-authoring',
        'Advanced SQL and chart authoring',
        'Let authorized analysts explore governed data and publish additional visualizations.',
        '/insights/usage/dashboards',
        'Open dashboard tools',
        'Define the analyst authorization boundary and either integrate bounded authoring or explicitly keep it in the separately authenticated Superset surface.',
        [
          'yes',
          'Superset 4.1.1 provides SQL Lab, datasets, charts, and dashboards.',
          'partial',
          'The Console provisions its owned assets but does not wrap the full authoring lifecycle.',
          'partial',
          'Operators can link to Superset; authoring is not a native Off Grid CRUD surface.',
          'no',
          'No seeded analyst authoring workflow is verified.',
        ],
      ],
    ],
  }),
  audit({
    serviceId: 'fleetdm',
    serviceLabel: 'Device Management',
    upstreamVersion: '4.87.0',
    versionSource:
      'deploy/docker-compose.yml (fleetdm/fleet:v4.87.0); live docker ps on g6 verified 2026-07-23 — container offgrid-services-b-fleet-1 running the pinned v4.87.0 image',
    denominatorSource: 'https://fleetdm.com/docs/rest-api/rest-api',
    auditState: 'current',
    summary:
      'Relevant Community-edition denominator: host inventory, live/saved queries, software/CVE visibility, and policies. Premium MDM control is explicitly unsupported without licensing.',
    capabilities: [
      [
        'host-inventory-software',
        'Host and software inventory',
        'Inspect enrolled devices, installed software, and vulnerability context.',
        '/operations/devices',
        'Manage devices',
        'Complete operator setup, enroll a real test host, and prove inventory refresh.',
        [
          'yes',
          'Fleet 4.87.0 Community provides host and software inventory.',
          'yes',
          'Fleet adapters map hosts and software inventory.',
          'yes',
          'Operations device list and detail routes expose inventory.',
          'no',
          'The live fleet has no operator-owned enrollment evidence.',
        ],
      ],
      [
        'queries-policies',
        'Live queries and compliance policies',
        'Run bounded osquery queries and manage host compliance checks.',
        '/operations/devices',
        'Manage device posture',
        'Prove one safe query and one policy lifecycle against an enrolled live host.',
        [
          'yes',
          'Fleet 4.87.0 Community provides queries, campaigns, and policies.',
          'yes',
          'Adapters and routes implement live query and policy CRUD.',
          'yes',
          'Device surfaces expose query and policy actions.',
          'no',
          'No enrolled-host query or policy evidence is recorded.',
        ],
      ],
      [
        'premium-mdm',
        'Full MDM device control',
        'Push configuration profiles, escrow disk-encryption keys, enforce OS updates, and automate enrollment.',
        '/operations/devices',
        'Inspect device controls',
        'Deliberately not pursued (open-core boundary). Fleet is dual-licensed: inventory, live queries, policies, scripts, and remote lock/wipe are MIT (usable free), but the richer MDM surface — configuration-profile push, FileVault/BitLocker key escrow, OS-update enforcement, automated (ADE/DEP) enrollment, Platform SSO — lives under Fleet /ee and is gated by an IsPremium() license check (paid EE subscription). We do not ship against another vendor\'s paid tier, so full MDM stays unavailable by choice, not by gap.',
        [
          'no',
          'Verified against the Fleet source 2026-07-24: /ee/server/service/apple_mdm.go + ~28 license.IsPremium()+MDM code gates confine the full-MDM surface to the commercial EE license (ee/LICENSE: production use requires a paid Fleet EE subscription). The MIT tier does not establish it.',
          'partial',
          'Adapters model the device-control commands but gate the EE-only controls as unavailable.',
          'partial',
          'The UI explains the open-core unavailability without presenting a non-functional control.',
          'no',
          'Not pursued: shipping EE-licensed MDM would require a paid Fleet subscription, against our open-core principle. Free lock/wipe (MIT) could be surfaced separately if a device-control use case arises.',
        ],
      ],
    ],
  }),
  {
    serviceId: 'presidio',
    serviceLabel: 'Presidio',
    upstreamVersion: '2.2.356',
    versionSource: 'deploy/docker-compose.yml',
    denominatorSource: 'https://microsoft.github.io/presidio/api-docs/',
    auditedAt: '2026-07-19',
    auditState: 'current',
    auditStateEvidence: null,
    summary:
      'Text analysis, replacement anonymization, org recognizers, deny lists, and thresholds are integrated into the data-redaction path. Language and advanced anonymizer operations remain narrow.',
    items: [
      defineCapability(
        'text-analysis',
        'Text entity analysis',
        'Detect built-in PII entities and return typed spans with confidence scores.',
        '/governance/guardrails/test',
        'Test live detection',
        '',
        [
          'yes',
          'The analyzer image exposes /analyze.',
          'yes',
          'scanWithPresidio posts a typed analyze request and applies returned spans.',
          'yes',
          'The Guardrails test surface runs the active engine.',
          'yes',
          'The selected data-redaction adapter runs in app and data-movement policy checks.',
        ],
      ),
      defineCapability(
        'replace-anonymization',
        'Replacement anonymization',
        'Send analyzed spans to the anonymizer and replace values with entity markers.',
        '/governance/guardrails/test',
        'Test live redaction',
        '',
        [
          'yes',
          'The anonymizer image exposes /anonymize with replace operators.',
          'yes',
          'The adapter calls analyzer then anonymizer and has a span-safe local fallback.',
          'yes',
          'The test surface displays the redacted result and active engine.',
          'yes',
          'Data-redaction workflows consume the returned redacted text.',
        ],
      ),
      defineCapability(
        'pattern-recognizers',
        'Custom pattern recognizers',
        'Create org-scoped regex and context recognizers and send them per analyze request.',
        '/governance/guardrails/recognizers',
        'Manage recognizers',
        '',
        [
          'yes',
          'Presidio accepts ad_hoc_recognizers with regex patterns and context.',
          'yes',
          'Stored recognizers are normalized and merged into every analyze request.',
          'yes',
          'The recognizers route provides create, update, enable, and delete controls.',
          'yes',
          'The same org policy is loaded by the real data-redaction adapter.',
        ],
      ),
      defineCapability(
        'deny-lists',
        'Custom deny lists',
        'Flag fixed org-specific terms as a named PII entity.',
        '/governance/guardrails/recognizers',
        'Manage deny lists',
        '',
        [
          'yes',
          'PatternRecognizer supports deny_list values.',
          'yes',
          'Deny lists are translated into enabled ad hoc recognizers.',
          'yes',
          'The recognizer manager supports deny-list CRUD.',
          'yes',
          'Enabled deny lists are loaded into production analyze requests.',
        ],
      ),
      defineCapability(
        'confidence-thresholds',
        'Global and per-entity thresholds',
        'Set an org floor and stricter or looser thresholds by entity type.',
        '/governance/guardrails/thresholds',
        'Manage thresholds',
        '',
        [
          'yes',
          'The analyzer accepts a request score threshold.',
          'yes',
          'The global floor is sent upstream and per-entity thresholds are enforced on returned scores.',
          'yes',
          'The thresholds route edits the org policy.',
          'yes',
          'The real adapter loads and applies the org threshold policy.',
        ],
      ),
      defineCapability(
        'languages',
        'Language selection and multilingual analysis',
        'Choose a supported language and recognizer set for each analysis request.',
        PRESIDIO_ROUTE,
        'Inspect Presidio status',
        'CORRECTED 2026-08-04 — the previous instruction ("add an org or pipeline setting") would have BROKEN PII protection on this deployment. Probed live: /supportedentities answers 200 for en and 500 "No matching recognizers were found to serve the request" for hi, ta, mr and es. English is the only language served. This scan is fail-closed, so a picker offering Hindi would send hi, the analyzer would 500, and every governed call would be refused. The validation therefore shipped and the picker deliberately did NOT. What remains open is genuinely upstream: install spaCy models for the languages a tenant needs, and only then is a setting meaningful.',
        [
          'yes',
          'Presidio supports language-specific recognizers.',
          'yes',
          'presidioLanguages() DISCOVERS support by probing (there is no list-languages endpoint: a served language answers 200, an unserved one 500), cached 10 minutes, and resolveAnalyzerLanguage never returns a language the service cannot serve — it substitutes the default and states why, because a scan that quietly ran in the wrong language is a scan whose misses nobody can account for. Verified live: requesting hi on Devanagari text returns PERSON, IN_PAN, IN_AADHAAR at engine=presidio status=applied instead of a refused call.',
          'no',
          'No language control on the Guardrails surface, and deliberately so while one language is served — a picker with one valid option invites the invalid choice. describeLanguageReach states the reach instead: Indian identifiers are matched by PATTERN and are found whatever script surrounds them (verified on Devanagari), and only names and places in another script are matched less reliably.',
          'no',
          'Production scans run in English. Not a defect on this deployment — it is the only language installed — but it is the honest statement rather than an implied multilingual capability.',
        ],
      ),
      defineCapability(
        'advanced-anonymizers',
        'Mask, hash, encrypt, redact, and custom operators',
        'Apply an anonymizer operator per entity instead of fixed replacement markers.',
        '/governance/guardrails/masking',
        'Open masking policy',
        '',
        [
          'yes',
          'Presidio anonymizer ships multiple operator types (replace/redact/mask/hash/encrypt/keep verified against the deployed 2.2.356 API).',
          'yes',
          'The production data-redaction path (getDataRedactionPii -> scanWithPresidio) builds the /anonymize body from the org OPERATOR POLICY via the shared pure buildAnonymizeRequest, and binds the encrypt key from the secrets store (bindEncryptKey) only when the policy encrypts. Key material is never persisted: a keyless encrypt spec means "use the vaulted key", and an unresolvable key FAILS SAFE by downgrading to masking (never plaintext).',
          'yes',
          'Governance > Guardrails > Masking edits the per-entity operator policy and manages the AES key lifecycle (generate/rotate) through an admin route that stores it server-side in OpenBao and never returns it; the old plaintext key input was removed.',
          'yes',
          'Fleet-proven live 2026-07-25 on the production data-movement path (POST /api/v1/admin/data-redaction, engine=presidio). BEFORE the fix the same request returned the hard-coded "Customer PAN [IN_PAN]" while the org policy said mask — AFTER it returns "Customer PAN ******234F" (configured mask), Aadhaar returns "Aadhaar  on file" (configured redact), and with IN_PAN switched to a keyless encrypt spec it returned real ciphertext "emk1bSCMVhv/Ib/9ieZDKSQh0xnH70/Bwc1h+jOwiiU=" from the vault-held key, with the stored policy row containing {"type":"encrypt"} and NO key. Policy restored to mask afterwards. Scope note: this capability is per-entity operator SELECTION, which is complete; RECOVERING an encrypted original (Presidio /deanonymize) is a separate capability with no adapter or authorized surface yet, tracked in GAPS_BACKLOG (G-PRESIDIO-DEANONYMIZE).',
        ],
      ),
      defineCapability(
        'image-redaction',
        'Image PII redaction',
        'Detect and redact PII in images and scanned documents.',
        '/governance/guardrails/masking',
        'Redact a document image',
        'Operator upload→redact→review is live and audited; no automatic app-step or pipeline invokes image redaction yet (a manual governed surface, not an in-line workflow step).',
        [
          'yes',
          'Presidio image-redactor 0.0.59 deployed on g6 (offgrid-console-presidio-image-redactor-1, :5003), reached via the S1 :8940 tunnel.',
          'yes',
          'createPresidioImageRedactor (src/lib/adapters/presidio-image-redaction.ts) posts to /v1/redact, contract-checks the engine/OCR/media response, and verifies the output bytes differ from the input. Live-verified 2026-07-24: a KYC PNG through /api/v1/governance/image-redaction returned HTTP 200, image sha CHANGED (4661→3559 bytes), PHONE_NUMBER redacted, receipt imgred_… + governance.image.redact audit row (outcome redacted).',
          'yes',
          'ImageRedactionPanel (Governance → Guardrails → Masking): upload a PNG/JPEG, redact via the governed route, review the redacted image + detected entities + tamper-evident receipt. Availability resolves from the real adapter config, not a hardcoded flag.',
          'partial',
          'The governed API route + operator UI are live and audited, but no App step or pipeline stage invokes image redaction automatically on document ingress.',
        ],
      ),
    ],
  },
  {
    serviceId: 'litellm',
    serviceLabel: 'LiteLLM',
    upstreamVersion: 'main-stable (mutable image tag)',
    versionSource: 'deploy/docker-compose.yml',
    denominatorSource: 'https://docs.litellm.ai/docs/',
    auditedAt: '2026-07-19',
    auditState: 'current',
    auditStateEvidence: null,
    summary:
      'The router config generator, management read-back, and inference endpoint seam are built. The live model-door cutover and callback delivery remain unverified, so routing and enforcement do not count as production use.',
    items: [
      defineCapability(
        'openai-proxy',
        'OpenAI-compatible inference proxy',
        'Serve chat and model requests through a single OpenAI-compatible model door.',
        ROUTER_ROUTE,
        'Inspect inference wiring',
        'PARTLY PROVEN 2026-08-04. A live round-trip through the proxy now exists: POST 127.0.0.1:4000/v1/chat/completions with model onprem/qwen3-vl-8b returned the requested string ("PROXY-CUTOVER-OK", 19 prompt + 8 completion tokens, system_fingerprint b1-277a105), and the proxy ATTRIBUTED it — /spend/logs carries 47 rows dated 2026-08-04 with the newest at 09:45:51 matching that request. This was impossible before today: the loopback forward on :4000 was a legacy root gost process hard-coding 192.168.1.65 while g5 had moved to .29, so the port was bound and every request timed out (fixed by adopting the forward into the repo-owned mDNS tcp-forward.js). STILL OPEN, and deliberately not claimed: console CHAT and a GOVERNED PIPELINE run have not been routed through the proxy — inference currently goes through the aggregator on :8800, and repointing it is a deployment change, not a console one. So the proxy is proven reachable and attributing; it is not proven as the serving path for product traffic.',
        [
          'yes',
          'LiteLLM Proxy exposes OpenAI-compatible inference APIs.',
          'yes',
          'The gateway endpoint seam resolves LiteLLM URL and bearer credentials.',
          'yes',
          'The Routing view shows the selected inference door.',
          'partial',
          'Production request paths support the seam, but live LiteLLM cutover is not verified.',
        ],
      ),
      defineCapability(
        'load-balance-failover',
        'Load balancing, retries, and failover',
        'Route a model alias across fleet and cloud deployments with health-aware retry policy.',
        ROUTER_ROUTE,
        'Inspect routing health',
        'FAILOVER IS PROVEN; REDUNDANCY IS NOT CONFIGURED. The production config has exactly one deployment per model name, so there is nothing to fail over to — a single inference host going down still takes its model with it. Closing that means a second host serving the same model (G-210, owner: fleet repo), not more console work.',
        [
          'yes',
          'LiteLLM Router supports deployment groups, health-aware balancing, retries, and fallbacks.',
          'yes',
          'The generated config defines model groups, retry policy, and deployment metadata.',
          'yes',
          'The Routing view displays deployment health.',
          'yes',
          'FAILURE DRILL RUN LIVE 2026-08-05: a dead second deployment was added under onprem/qwythos-9b and requests were retried onto the healthy peer and completed — attempted-retries 1-2 with the response served by the healthy deployment id, which is the router selecting the dead peer, failing against it, and finishing elsewhere. Pool restored and re-verified afterwards. NOTE THE TRAP: the first attempt used six identical prompts and every one returned 200, INCLUDING one naming the DEAD deployment as its server, because response caching is enabled — that drill measured Redis, not the router. Any future routing or latency drill here must send unique prompts or no-cache. docs/evidence/2026-08-05-gateway-failover-drill.md',
        ],
      ),
      defineCapability(
        'deployment-inventory',
        'Deployment inventory and health',
        'Read configured models and merge them with healthy and unhealthy endpoint state.',
        ROUTER_ROUTE,
        'Inspect deployments',
        '',
        [
          'yes',
          'LiteLLM exposes model info and health endpoints.',
          'yes',
          'safeRouterView merges /model/info and /health without fabricating missing deployments.',
          'yes',
          'The Routing view renders deployment, model, egress, vision, and health.',
          'yes',
          'Operations can inspect the configured LiteLLM service independently of model-door selection.',
        ],
      ),
      defineCapability(
        'budgets-rate-limits',
        'Budgets and RPM/TPM limits',
        'Enforce spend and request limits on virtual keys.',
        ROUTER_ROUTE,
        'Inspect key budget',
        'Full key + budget/RPM/TPM CRUD is live over LiteLLM DB-backed FinOps, and REQUEST-RATE enforcement (RPM) is fleet-verified. IMPORTANT honesty caveat: LiteLLM dollar-budgets are cost-based, so they only bite when the routed model carries a per-token price — on the on-prem/free models the demo runs, every call computes $0 spend, so a dollar max_budget never trips (verified: two calls on a $1e-7-budget key both returned 200 and key spend stayed 0.0). For free local models RPM/TPM are the effective control; dollar-budgets bind cloud/cost-bearing routes. Next: register model cost for cost-bearing routes + surface per-key spend trends + audit records on key mutations.',
        [
          'yes',
          'LiteLLM supports per-key budget, RPM, and TPM controls.',
          'yes',
          'The adapter creates/updates/deletes/lists keys with max_budget + rpm/tpm via LiteLLM /key/generate|update|delete|list (litellm-key-policy builds the bodies + shapes rows).',
          'yes',
          'The Routing view Virtual-keys panel manages budgets + RPM/TPM (create, edit-budget, revoke) with live spend/utilization + over-budget badges.',
          'partial',
          'REQUEST-RATE enforcement is fleet-proven live: a key with rpm_limit=2 admitted requests 1-2 (HTTP 200) and BLOCKED requests 3-6 (HTTP 429 "Rate limit exceeded ... Current limit: 2, Remaining: 0"). But the SPEND half of this capability does NOT enforce on the demo routes: dollar-budget enforcement was NOT observed on free on-prem models — a $1e-7-budget key served calls with spend stuck at 0.0 (no model cost → no spend → budget never trips). So the workflow is partial: RPM/TPM bind, dollar-budgets are a $0 no-op until cost-bearing/cloud routes carry a per-token price.',
        ],
      ),
      defineCapability(
        'virtual-keys',
        'Virtual key lifecycle',
        'Create, rotate, revoke, scope, and inspect LiteLLM virtual keys.',
        ROUTER_ROUTE,
        'Inspect current key snapshot',
        '',
        [
          'yes',
          'LiteLLM exposes virtual key management APIs.',
          'yes',
          'The adapter runs the full lifecycle — generateVirtualKey / listVirtualKeys / updateVirtualKey / deleteVirtualKeys — over LiteLLM /key/*.',
          'yes',
          'The Virtual-keys panel creates (alias + budget + RPM/TPM), edits, and revokes keys; the new token is shown once, list rows show only the masked token.',
          'yes',
          'Fleet-proven live: keys created + listed + revoked through the console admin routes against LiteLLM DB-backed /key/* (litellm-database image; requires the DB enabled, now codified in deploy-g5-model-router.sh).',
        ],
      ),
      defineCapability(
        'provider-pools',
        'Fleet and cloud provider pools',
        'Generate deployment entries for on-prem nodes and approved cloud providers.',
        '/runtime/models/providers',
        'Inspect model providers',
        'The generator consumes fleet and environment inputs, but the console does not own a validated LiteLLM config publish or reload transaction.',
        [
          'yes',
          'LiteLLM supports many providers behind model aliases.',
          'yes',
          'buildLiteLLMConfig maps fleet nodes and cloud providers to model_list entries.',
          'partial',
          'Provider status is visible, but it is not a LiteLLM pool editor.',
          'no',
          'No production workflow publishes and reloads generated config through the console.',
        ],
      ),
      defineCapability(
        'structured-callbacks',
        'Structured request logging callbacks',
        'Send completion outcomes, deployment, tokens, caller, latency, and status into traffic records.',
        '/runtime/models/callbacks',
        'Open callback stream',
        'Runtime add/remove of a callback sink is config-owned on the deployed LiteLLM version (set at deploy + reload); the console reads and surfaces the active sinks honestly and manages them where the version allows.',
        [
          'yes',
          'LiteLLM emits StandardLoggingPayload callbacks and exposes its configured success/failure callback sinks.',
          'yes',
          'The litellm-callbacks adapter reads the live proxy callback config + health and normalizes the StandardLoggingPayload shape (litellm-log-shape). Verified live: reachable+active, real sinks (_ProxyDBLogger, deployment_callback_on_success).',
          'yes',
          'AI Runtime → Models → Callbacks shows active sinks, health, and a per-call payload preview.',
          'partial',
          'Fleet-proven live: the deployed LiteLLM reports its active callback sinks through the console; enabling a NEW sink (e.g. Langfuse/OTel) is a proxy-config + reload step on this version.',
        ],
      ),
      defineCapability(
        'spend-analytics',
        'Spend and cost analytics',
        'Attribute request cost by key, model, deployment, team, and time window.',
        '/runtime/models/spend',
        'Open gateway spend',
        '',
        [
          'yes',
          'LiteLLM (DB-backed proxy) tracks per-request spend and exposes it over /spend/logs (+ optional /global/spend/keys|models rollups); the console additionally meters the audit/traffic log with per-model pricing (finops.priceFor/costForTokens).',
          'yes',
          'Two adapters cover it: assembleFinOps buckets real per-request token counts × price into cost by model/user/project, and adapters/litellm-spend.ts reads LiteLLM /spend/logs and rolls up by model/virtual-key/time-bucket in the pure litellm-spend.ts (window parsing, normalization, aggregation, summary — unit-tested).',
          'yes',
          'Insights → Cost (Models/Users/Projects/Overview) attributes spend with pipeline + time-window filters, and the new gateway FinOps surface at /runtime/models/spend attributes spend/tokens/requests by model, by virtual-key, and over time (24h/7d/30d) with a per-request drill-down — honestly leading with tokens/volume and labelling $0 on free on-prem models.',
          'yes',
          'Fleet-proven live on both paths. Console-native: cost-by-model shows real usage (Qwen 2.5 14B — 53 requests, 74,808 tokens; GPT-4o-mini — 4 requests) priced into spend, filterable by pipeline + window; local models correctly report $0. LiteLLM-ledger (verified 2026-07-24): /api/v1/admin/gateway/spend/{by-model,by-key,logs} all returned configured:true live:true with real /spend/logs rows from the DB-backed proxy — honestly leading with request volume/tokens, labelling $0 on free on-prem models, and marking unattributed (master-key) calls as such rather than fabricating a virtual-key.',
        ],
      ),
      defineCapability(
        'response-cache',
        'Response caching',
        'Cache eligible model responses with explicit privacy, scope, and expiry policy.',
        '/runtime/models/cache',
        'Open response cache',
        'Caching is enabled + healthy and a real hit/flush is live-proven at the proxy; the only gap is the CONSOLE hit-rate metric, which this LiteLLM version leaves unpopulated (it does not write cache_hit rows to /spend/logs), so effectiveness reads "not reported" while status + flush are live.',
        [
          'yes',
          'Response caching is ENABLED + HEALTHY on the g5 LiteLLM proxy against Redis (offgrid-g6.local:6379, namespace offgrid, ttl 3600). Verified live 2026-07-24: /cache/ping → {status:healthy, cache_type:redis, ping_response:true, set_cache_response:success}.',
          'yes',
          'The litellm-cache adapter calls /cache/ping (status/type/policy), /cache/flushall|/cache/delete (flush), and derives hit-rate/tokens-saved from /spend/logs cache_hit markers — verified live reaching the real proxy and surfacing the ENABLED healthy state.',
          'yes',
          'AI Runtime → Models → Cache shows live cache status (redis, healthy), flush actions, and cache effectiveness with an honest "not reported" when the proxy emits no cache_hit spend marker.',
          'partial',
          'The enable→HIT→flush→miss workflow is live-proven at the proxy: an identical completion re-served from Redis in 32 ms vs 2.85 s cold (89x), carrying x-litellm-cache-key; /cache/flushall → {status:success} then the same request MISSES again. The console cache surface is configured:true live:true. Remaining gap: this LiteLLM version (1.93.0) does not write cache_hit rows to /spend/logs, so the console-derived hit-RATE metric shows "not reported" (markerUnavailable) even though caching demonstrably works — surfaced honestly, not faked.',
        ],
      ),
      defineCapability(
        'proxy-guardrails',
        'Proxy guardrails and policy hooks',
        'Run supported guardrails in the proxy before or after model calls.',
        ROUTER_ROUTE,
        'Inspect router capabilities',
        'DELIBERATE, not missing. Governance lives in the Off Grid pipeline spine — policy, guardrails, grounding, masking and the egress leash run there, per-run and per-tenant, with every decision recorded against the run. Moving any of it into the proxy would SPLIT the enforcement point: two places to configure, two to audit, and no single answer to "what governed this run". The proxy stays a transport. This is an ownership choice, not open build work, and the item should be read that way rather than as a gap awaiting code.',
        [
          'yes',
          'LiteLLM supports proxy guardrail integrations.',
          'no',
          'No LiteLLM guardrail is configured or adapted — DELIBERATELY; see the gap.',
          'no',
          'The Router view has no proxy guardrail controls.',
          'no',
          'Production governance runs in the Off Grid policy and guardrail spine instead.',
        ],
      ),
    ],
  },
] as const satisfies readonly ServiceCapabilityAudit[];

/**
 * These services belong to this lane but still lack a versioned upstream denominator. Keeping the
 * IDs explicit proves inventory coverage while leaving the canonical capability summary honestly
 * `not-audited`.
 */
export const RUNTIME_GOVERNANCE_OPERATIONS_UNAUDITED_SERVICE_IDS = [] as const;

export const RUNTIME_GOVERNANCE_OPERATIONS_SERVICE_IDS = [
  ...RUNTIME_GOVERNANCE_OPERATIONS_AUDITS.map((record) => record.serviceId),
  ...RUNTIME_GOVERNANCE_OPERATIONS_UNAUDITED_SERVICE_IDS,
] as const;
