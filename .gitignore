# dependencies
/node_modules

# next
/.next/
/out/
/build
next-env.d.ts
*.tsbuildinfo

# component catalog — cloned for DISCOVERY only, never committed (see README)
/vendor/component-library-animations/

# env
.env*.local
.env.production

# misc
.DS_Store
*.pem
npm-debug.log*

# c8 / coverage reports
/coverage/
.nyc_output/

# LanceDB (Brain vector store)
/.lancedb/
.claude/worktrees/
deploy/onprem/CREDENTIALS.md

# ragas sidecar compose secret (real gateway key)
deploy/onprem/ragas-sidecar/.env

# Python bytecode
__pycache__/
*.pyc
.shots/

# Confidential client-derived material — MUST NOT be committed (scrubbed 2026-07-12)
# Requirement docs / use-case plans / seed data derived from a client's confidential brief
# stay local only. Keep only the FICTIONAL demo tenant (Suraksha/bharatunion) seeds in git.
docs/EY_USE_CASES_PLAN.md
**/*-ey-seed.ts
**/ey-usecase*
**/data-domains-ey-seed*
